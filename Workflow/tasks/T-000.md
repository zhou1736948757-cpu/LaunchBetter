# Task T-000 — Fresh workflow initialization (v1.4, from scratch)

- **Objective**: Initialize a fresh `chef-worker-reviewer-workflow` contract for LaunchBetter at v1.4 after the user-requested fresh start (previous run archived to `Workflow一/`).
- **Owner**: Main (current main conversation)
- **Acceptance criteria**:
  1. `Workflow/config.json` v1.4 schema: models `current-main-conversation` for main/chief/worker/reviewer, `max_worker_concurrency` 3, `thinking_depth` high.
  2. `Workflow/manifest.json` v1.4 with role assignments (main/chief/worker/reviewer) and artifact paths.
  3. `Workflow/STATE.json` status `READY`, empty task ledger.
  4. `Workflow/PLAN.md` / `Workflow/MAIN_BRIEF.md` placeholders present.
  5. `AGENTS.md` managed section regenerated; project content preserved.
  6. `MEMORY.md` preserved; runtime-config block consistent; current-state section updated to fresh start.
  7. Previous run fully archived in `Workflow一/` (with `ARCHIVE-NOTE.md`); legacy `.workflow/` removed after merge.
- **Verification**: artifact inspection; `git status` shows expected modified/untracked files.
- **Reviewer focus**: contract consistency; no project memory or archive content lost.
