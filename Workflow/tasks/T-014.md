# Task T-014 — Final workflow state reconciliation

- **Objective**: record-only 一致性修复。修正 T-013 收口后 `Workflow/STATE.json`、`Workflow/events.jsonl`、`MEMORY.md` 之间的记录矛盾，补齐配置 supersession 决策与 B1 证据范围说明。**不修改任何生产/测试代码，不重新解释 T-013，不重开分页架构，不调整默认手感，不执行 commit/push/tag/release。**
- **Owner**: Main（编排 + STATE + events）；Worker（有界记录工作）；Reviewer（独立审核）
- **Runtime**: Worker concurrency 1 / Reviewer concurrency 1 / 串行；不创建 PLAN.md/MAIN_BRIEF.md；不修改模型配置（读取并复用 `Workflow/config.json`）；仅当必须改变工作流契约或 STATE schema 时才升级 Chief。

## 已确认事实（不得重新争论或降级）

1. T-013-A 真实末页 active-settle interruption 已通过；2. T-013-B telemetry 集成测试已通过；3. LaunchCore 200/16 PASS；4. LaunchUI 470/66 PASS；5. LaunchPlatform 149/23 PASS；6. GridIntegration 33/33 PASS；7. `git diff --check` PASS；8. T-013 无新增生产 Swift 修改；9. T-013 Reviewer verdict = PASS；10. MANUAL_PHYSICAL_GATE 仍 OPEN/REQUIRED；11. 未执行 commit/push/tag/release。本轮不得把自动化 PASS 表述为真机手感 PASS。

## 1. 修改前快照（Main 已建立，2026-08-26T15:5x）

- git：`HEAD == origin/main == 843ba1f1ec41c0328b16a9e5401dc94ab837c325`（branch main）；dirty worktree 含 13 个 modified + untracked（含 `Workflow/`、`Workflow一/`）；`git diff --check` exit 0；diff --stat 13 files / 2316+/109-。
- `Workflow/STATE.json`（修改前）：`workflow_status READY`；`T-000.status READY`（attempts 0/0）；`T-013.status PASSED` 但 `worker_attempts 0`、`review_attempts 0`；`active_agent_jobs []`；`blockers []`；`latest_runtime_update 2026-08-26T15:52:18+09:00`。
- `Workflow/events.jsonl`（修改前）：共 6 行，最后 sequence 6 = `task_closed T-013`（2026-08-26T15:52:18+09:00）。现有事件：workflow_initialized、decision_recorded、task_created(T-013)、worker_completed(T-013)、review_passed(T-013)、task_closed(T-013)。**没有** worker_dispatched / review_started / state_changed 事件。
- `MEMORY.md`（nl -ba 核对）：`### Current state` 526；`### Task ledger` 534（表 534–545；T-000 538、T-008 539、T-009 540、T-010 541、T-011 542、T-012 543、T-013 544、T-007 545）；work-log start 553；T-012 work-log 条目 604–609；T-007 corrected closure 611–616；T-013 closure 618–623；work-log end 624。
- `Workflow/tasks/T-000.md` 原始验收条件第 1 条：main/chief/worker/reviewer 均 `current-main-conversation`、`max_worker_concurrency 3`、`thinking_depth high`。
- `Workflow/config.json`（当前实际）：main=`current-main-conversation`、chief=`ollama/glm-5.2`、worker=`ollama/deepseek-v4-flash:0731`、reviewer=`ollama/minimax-m2.7`、`max_worker_concurrency 2`、`thinking_depth max`；`configured_at 2026-08-26T13:48:33+09:00`。

## 2. 需要修正的矛盾（五个问题）

### P1: STATE 与 MEMORY/events 不一致
证据：T-000 初始化已完成（events sequence 1 workflow_initialized；MEMORY ledger T-000=PASSED）；T-013 Worker 实际执行过一次（events worker_completed）；T-013 Reviewer 实际执行过一次（events review_passed）；task_closed 事件写 T-013 PASSED / workflow PASSED；MEMORY 当前状态写 workflow PASSED。
修正（Main 负责，直接更新 `Workflow/STATE.json`）：
- `workflow_status` → `PASSED`（终态；执行 T-014 期间可暂为 ACTIVE，收口时置 PASSED）
- `T-000.status` → `PASSED`（保留字段结构与最新更新时间戳）
- `T-013.worker_attempts` → `1`；`T-013.review_attempts` → `1`（依据已有完成证据重建，见 P2 事件说明）
- 不得虚构未发生过的调度（不能声称过去记录过 dispatch/start 时间）。

### P2 events 缺少完整调度事件
现有 events 只有 task_created / worker_completed / review_passed / task_closed。**不得插入或修改旧 JSONL 行；不得伪造过去时间线。** 修正：Main 追加"状态重建/记录修正"事件（event_type 用 `state_changed` 或 `note`，action 明确说明）：
- attempt counters 根据已有完成证据（worker_completed / review_passed 事件）重建为 1；
- 明确声明 T-013 的 worker_dispatched / review_started 当时未记录，**不**追溯声称其存在；
- 后续 T-014 自身的生命周期事件（task_created / worker_dispatched / worker_completed / review_started / review_completed / review_passed / task_closed）在真实发生时正常追加。

### P3 MEMORY 行号 Addendum 已漂移
- 归档 `Workflow一/reviews/T-012-r2.md` 尾部 Addendum（2026-08-26, T-013 C3）引用的行号（ledger 534–544、T-010 541、T-012 543、T-007 544、marker 552、T-012 条目 603–608、T-007 条目 610–614）因 T-013 收口新增 ledger 行与 work-log 条目已漂移。
- **不删除/重写旧 Addendum**；在其后追加新的校正说明（`## Addendum 2 (2026-08-26, T-014)`），记录：
  - 当前快照行号（以本包 §1 为准，但 Worker 完成后必须用 `nl -ba MEMORY.md` 复核；若与快照不符，以复核结果为准并写明）；
  - 稳定 section/row 标识（如 `### Task ledger` + `T-013` 行、`work-log:start` marker、`### 2026-08-26T15:55:00+09:00 — T-013 closure` 标题）；
  - 声明"行号是 2026-08-26 快照，未来以 section heading + Task ID 为准"。

### P5 T-000 历史任务包与当前配置不同
- `Workflow/tasks/T-000.md` 验收条件第 1 条写旧配置（全 current-main-conversation / concurrency 3 / thinking high）；当前 `Workflow/config.json` 为用户确认后的新配置（见 `Workflow/decisions/2026-08-26-model-configuration.md`）。
- **不得重写 T-000 原始任务包**。新增 supersession decision：`Workflow/decisions/T-000-configuration-supersession.md`，说明 T-000 初始模型配置要求已被后续用户确认配置取代（引用 model-configuration decision 与 config.json configured_at），并注明 T-000 其余初始化验收条件不受影响。

### P5 B1 名称/描述略有过度声明
- 测试名 `startSettleReplacementKeepsTelemetrySessionsIsolated`。实际 Grid E2E 路径：第二次 `pagingProbeGesture` → `beginGesture()` 打断旧 settle → phase 进入 tracking → `endGesture()` → `startSettle()`。因此它验证"真实手势中断后的 telemetry session 隔离"，但**不是直接从 `.settling` phase 进入 `startSettle()` 的 replacement branch**（直接 `startSettle(.settling)` lifecycle 已有 controller-level 测试）。
- **不重命名测试、不修改测试文件**。只在 canonical 记录中追加准确说明：
  - Grid E2E 验证 gesture interruption session isolation；
  - controller-level test 验证 direct startSettle replacement lifecycle；
  - 两者合并构成完整证据；
  - 不得再表述为"Grid E2E 直接进入 startSettle(.settling) branch"。
- 追加位置（仅 append）：`Workflow/results/T-013.md` 与 `Workflow/reviews/T-013-r1.md` 各加一个 Addendum 节；如相关描述出现在归档 `Workflow一/reviews/T-012-r2.md`，同样追加说明（不改原文）。

## 3. 可写文件（Expected writable files）

- `Workflow/STATE.json`（Main 更新）
- `Workflow/events.jsonl`（Main，仅 append）
- `Workflow/results/T-014.md`（Worker 结果）
- `Workflow/reviews/T-014-r1.md`（Reviewer 写）
- `Workflow/decisions/T-000-configuration-supersession.md`（Worker 写）
- `Workflow/results/T-013.md`（仅追加 Addendum）
- `Workflow/reviews/T-013-r1.md`（仅追加 Addendum）
- `Workflow一/reviews/T-012-r2.md`（仅追加 Addendum 2）
- `MEMORY.md`（Worker 追加 Addendum/ledger/work-log；Main 收口时更新 ledger 状态）
- 必要时 `Workflow/README.md`（若需补充 STATE 状态语义说明；非必须则不写）

## 4. 禁止修改（Forbidden files）

- `Packages/**`、`Docs/**`、`App/**`、`Tests/**`、`Package.swift`、`project.pbxproj`
- `AGENTS.md`（除非发现 canonical contract 明确错误且 Main 先记录原因）
- `Workflow/config.json`、`Workflow/manifest.json`
- 旧 `events.jsonl` 行（只允许 append）
- 历史 FAIL 原文（`Workflow一/reviews/T-010-r1.md` 等）
- 归档 `Workflow一/` 其他文件（除 T-012-r2.md 追加 Addendum 2 外）

## 5. Acceptance criteria（Worker 完成后逐条自检；Reviewer 逐条核对）

1. STATE.json 与 MEMORY/events 一致：workflow_status PASSED（终态）、T-000 PASSED、T-013 attempts 1/1、T-014 终态 PASSED attempts 1/1；无 READY 任务与 PASSED workflow 并存。
2. events.jsonl 仅 append；存在"状态重建"事件（state_changed/note）明确说明 attempt 重建依据与"未追溯记录 dispatch/start"；T-014 生命周期事件齐全且真实（task_created → worker_dispatched → worker_completed → review_started → review_passed → task_closed）。
3. `Workflow一/reviews/T-012-r2.md` 追加 Addendum 2：包含最新 `nl -ba MEMORY.md` 行号、稳定 section/row 标识、"行号是 2026-08-26 快照，未来以 section heading + Task ID 为准"；旧 Addendum 原文保留。
4. `Workflow/decisions/T-000-configuration-supersession.md` 存在：说明 T-000 初始配置被用户确认配置取代，引用 model-configuration decision 与 configured_at，明确不重写 T-000。
5. `Workflow/results/T-013.md` 与 `Workflow/reviews/T-013-r1.md` 各追加 Addendum：B1 证据范围准确说明（gesture interruption session isolation + controller-level direct startSettle replacement = 完整证据；不再表述为 Grid E2E 直接进入 startSettle(.settling) branch）；原文未改。
6. `Workflow/results/T-014.md`（Worker 结果）：Status / 修改前快照 / P1–P5 逐项修正说明（含实际命令、nl -ba 输出、行号） / 变更文件清单 / 禁止文件未被触碰确认 / deviations / unresolved issues / Reviewer focus / MANUAL_PHYSICAL_GATE / 未执行 commit/push/tag/release。
7. `MEMORY.md`：ledger 更新（T-014 行 PASSED + 若有需修正的引用）、work-log 增加 T-014 收口条目（Main 在 Reviewer PASS 后写）；当前状态保持 workflow PASSED + MANUAL_PHYSICAL_GATE OPEN。
8. 生产/测试代码零改动：`git diff --stat` 相对本轮开始无新增 Packages 变更；`git diff --check` PASS。
9. 不得把自动化 PASS 表述为真机手感 PASS。

## 6. 验证命令

- `git status --short --branch`、`git rev-parse HEAD`、`git rev-parse origin/main`、`git diff --check`、`git diff --stat`（前后对比）
- `nl -ba MEMORY.md` 抽查关键锚点
- `python3 -c "import json; s=json.load(open('Workflow/STATE.json')); print(json.dumps(s, indent=1))"` 校验终态
- `tail -12 Workflow/events.jsonl` 校验事件序列与只 append
- 不需要跑任何 swift test（record-only；如 Reviewer 需要可抽查结果文件）

## 7. Reviewer focus

- P1 STATE 终态与 events/MEMORY 一致（无 READY 任务 + PASSED workflow 并存）；
- P2 事件时间线诚实性：只有真实发生时记录的调度事件；状态修正事件措辞准确（不宣称过去记录过 dispatch/start）；
- P3 Addendum 2 行号与 `nl -ba` 实际输出一致、稳定标识正确、旧 Addendum 未删；
- P4 supersession decision 引用准确、不重写 T-000；
- P5 B1 说明准确（不夸大 Grid E2E 路径）；
- 禁止文件零改动（含生产/测试 Swift、config.json、manifest.json、AGENTS.md、旧 events 行）；
- `git diff --check` PASS、HEAD==origin/main、无 commit/push/tag/release；
- MANUAL_PHYSICAL_GATE 仍 OPEN / REQUIRED。

## 8. MANUAL_PHYSICAL_GATE

**OPEN / REQUIRED**：本任务为纯记录一致性修复，不涉及任何物理验证；自动化记录一致性 PASS 不关闭 MANUAL_PHYSICAL_GATE。

## 9. Commit / push / tag / release

**禁止**。HEAD 保持 == origin/main；dirty worktree 原样保留。

## 10. 角色分工

- Main：创建本包；STATE.json 修正（P1）；events 追加（P2 + T-014 生命周期）；派发 Worker；验证；派发 Reviewer；收口（MEMORY ledger/work-log 终态、STATE 终态、最终报告）。
- Worker：§5 之 3/4/5/6（Addendum 2、supersession decision、T-013/T-013-r1 Addendum、`Workflow/results/T-014.md`）+ §5-7 的 MEMORY ledger 初稿（T-014 行）。
- Reviewer：独立审核，写 `Workflow/reviews/T-014-r1.md`，verdict PASS/FAIL/BLOCKED。
- 只有出现"必须改变工作流契约或 STATE schema"才升级 Chief。
