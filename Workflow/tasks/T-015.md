# T-015 — T-014 验收表述纠正与生命周期记录纪律收口

你正在 `/Users/mac/Projects/LaunchBetter` 中继续执行
`chef-worker-reviewer-workflow` v1.4。

本任务为严格 record-only 任务。

不得修改任何生产 Swift、测试 Swift、Package.swift、Xcode 工程文件，
不得重新运行或改变 T-013 的生产结论，不得 commit、push、tag 或 release。

---

## 0. 角色与执行原则

提醒：

- Main 是当前主对话和 Runtime 编排中心。
- Chief 是独立、持久的规划与决策 Subagent。
- Worker 负责有界记录修改。
- Reviewer 负责独立审核，不得修改被审核记录。
- 本任务必须读取并复用 `Workflow/config.json`，不得改变模型、thinking depth
  或并发配置。
- 本任务 Worker concurrency = 1，Reviewer concurrency = 1，严格串行。
- 上游模型真实路由不可观察时，只能报告 configured identity，不得把本地配置
  当成最终路由证明。（如实记录：config.json 声明 chief=ollama/glm-5.2、
  worker=ollama/deepseek-v4-flash:0731、reviewer=ollama/minimax-m2.7；本
  harness 的 subagent 路由由会话默认路由决定，实际观测值在派发时如实报告。）

本任务涉及对 T-014 原验收条件结果的重新解释，因此必须先取得一份 Chief
Decision Delta。Main 不得自行弱化 T-014 验收条件。

---

## 1. 已确认事实（不得重新争论）

1. T-013 自动化实现与测试结论仍然有效。
2. LaunchCore 200、LaunchUI 470、LaunchPlatform 149、GridIntegration 33 的
   已记录结果不因本任务改变。
3. T-013 没有新增生产 Swift 修改。
4. MANUAL_PHYSICAL_GATE 仍然是 `OPEN / REQUIRED`。
5. T-014 已实质修正：
   - STATE 中 T-000/T-013/T-014 均为 PASSED；
   - T-013/T-014 attempts 为 1/1；
   - workflow_status 为 PASSED；
   - T-000 配置 supersession decision 已建立；
   - T-013 B1 的证据范围已经纠正。
6. 不得伪造、插入或重写历史 `events.jsonl` 事件。
7. 不得为了制造完整时间线而回填过去并未记录的
   `worker_dispatched` 或 `review_started`。

## 1.5 修改前快照（Main 已建立，2026-08-26T16:5x）

- git：`HEAD == origin/main == 843ba1f1ec41c0328b16a9e5401dc94ab837c325`；
  `git diff --check` exit 0；`git diff --stat` 13 files / 2324+/109−（既有
  dirty 基线，无新增 Packages 变更）。
- `Workflow/STATE.json`：`workflow_status PASSED`；T-000/T-013/T-014 全
  `PASSED`（T-013/T-014 attempts 各 1/1）；`active_agent_jobs []`；
  `blockers []`。
- `Workflow/events.jsonl`：12 行，seq 1–12。T-014 实际事件链：
  `task_created(7) → state_changed(8) → worker_completed(9) →
  review_passed(10) → state_changed(11) → task_closed(12)`；
  缺少 `worker_dispatched` 与 `review_started`。
- `MEMORY.md`：`### Current state` 526；`### Task ledger` 534（表 534–546；
  T-013 544、T-014 545、T-007 546）；work-log end 632。T-014 收口条目已在
  626 写入 → Addendum 2 的行号在 T-014 收口后已再次漂移。

---

## 2. 本任务要解决的两个问题

### P1 — T-014 AC2 并未严格满足

`Workflow/tasks/T-014.md` 的 Acceptance Criterion 2 明确要求：

`task_created → worker_dispatched → worker_completed → review_started →
review_passed → task_closed`

但当前 `Workflow/events.jsonl` 中 T-014 实际为：

`task_created → state_changed → worker_completed → review_passed →
state_changed → task_closed`

缺少：

- `worker_dispatched`
- `review_started`

现有 `state_changed` 已诚实声明这些事件当时没有记录，且没有回填伪造事件。
这保证了时间线诚实性，但不能证明 AC2 的"生命周期事件齐全"。

因此，以下表述不准确，必须纠正：

- "T-014 生命周期齐全"
- "T-014 7/7 无条件 PASS"
- "T-014 所有 Acceptance Criteria 全部满足"

### P2 — MEMORY 行号在 T-014 收口后再次漂移

`Workflow一/reviews/T-012-r2.md` 的 Addendum 2 在 Worker/Reviewer 检查时，
其行号快照是准确的。但 Main 随后向 `MEMORY.md` 添加 T-014 ledger/work-log，
导致行号再次变化。因此：

- Addendum 2 可以保留为"2026-08-26 当时的历史快照"；
- 不得再宣称其中行号是当前 live 行号；
- 后续引用必须优先使用稳定锚点：section heading、Task ID、
  work-log entry heading、managed marker；
- 不要继续用追加新行号 Addendum 的方式追逐动态行号。

---

## 3. Chief Decision Delta

Main 首先向 Chief 提交一个有界决策包，至少包含：

- `Workflow/tasks/T-014.md`
- `Workflow/results/T-014.md`
- `Workflow/reviews/T-014-r1.md`
- `Workflow/STATE.json`
- `Workflow/events.jsonl`
- `MEMORY.md`
- 本任务 P1/P2 的事实说明

Chief 必须输出：`Workflow/decisions/T-015-t014-acceptance-deviation.md`

推荐决策：

### ACCEPT_RECORDED_DEVIATION

含义：

1. 不回填或伪造 T-014 缺失的历史调度事件。
2. T-014 AC2 记为：timeline honesty：PASS；lifecycle completeness：
   NOT MET；accepted record deviation：YES。
3. T-014 可保持任务状态 `PASSED`，但只能表述为
   `PASSED WITH RECORDED DEVIATION`，不得表述为 7/7 全部满足。
4. 该接受必须明确是 Chief 的计划级决定，不能冒充原 AC2 已满足。
5. 此偏差仅限 T-014 历史记录，不改变未来任务的事件要求。
6. 从 T-015 开始，Main 必须在调度动作真实发生时写入完整事件，
   不得在任务结束后用解释事件替代调度事件。

如果 Chief 不接受该偏差，则必须返回 `REPLAN`，不能由 Main 静默保持
"T-014 无条件 PASS"。

---

## 4. T-015 自身必须使用完整真实事件链

T-015 必须证明未来生命周期记录纪律已经恢复。Main 必须在真实动作发生时，
按顺序 append：

1. `task_created`
2. `worker_dispatched`
3. `worker_completed`
4. `review_started`
5. `review_passed` 或 `review_failed`
6. `task_closed`

要求：

- `worker_dispatched` 必须在 Worker 开始执行前记录。
- `review_started` 必须在 Reviewer 开始审核前记录。
- 不得在 Worker/Reviewer 已经完成后补写成仿佛此前已经记录。
- sequence 严格单调；timestamp 不倒退；JSONL 每行独立有效；
  `events.jsonl` 只允许 append。
- 如果运行时遗漏其中任何一个事件，T-015 不得宣称完整 PASS。

（Chief 咨询为决策环节，不属于 Worker/Reviewer 事件链；Chief 决策落地后
Main 记录 `decision_recorded` 事件。）

---

## 5. Worker 修改范围

Worker 只允许修改或创建：

- `Workflow/decisions/T-015-t014-acceptance-deviation.md` —— 由 Chief 提供
  决策内容，Worker 可落盘但不得改变决策含义（若 Chief 已直接写盘则跳过）。
- `Workflow/results/T-014.md` —— 只追加 Addendum，不改原文。
- `Workflow/reviews/T-014-r1.md` —— Reviewer 原报告原则上不可由 Worker
  改写；若需追加，只能由独立 Reviewer 写 Reviewer Addendum。
- `Workflow一/reviews/T-012-r2.md` —— 只追加稳定锚点说明，不再写
  "当前 live 行号"。
- 必要时对仍声称旧行号为"当前准确值"的 canonical 记录追加说明：
  `Workflow/results/T-013.md`、`Workflow/reviews/T-013-r1.md`
- `Workflow/results/T-015.md`
- `Workflow/reviews/T-015-r1.md`（Reviewer 写）
- `Workflow/STATE.json`（Main 经脚本/直接更新）
- `Workflow/events.jsonl`，只允许 append（Main 执行）
- `MEMORY.md` —— Main 在 Reviewer PASS 后更新 ledger/work-log（Worker 不编辑）

禁止修改：

- `Packages/**`、`Docs/**`、`App/**`、`Tests/**`、`Package.swift`、
  `project.pbxproj`、`AGENTS.md`、`Workflow/config.json`、
  `Workflow/manifest.json`、T-000 原始任务包、T-014 原始任务包、
  旧 JSONL 行、历史 Reviewer 原文。

---

## 6. T-014 Addendum 必须说明

在 `Workflow/results/T-014.md` 追加：

`## Addendum — T-015 acceptance truthfulness correction`

至少写清：

1. T-014 的五项实质记录修复大部分成立。
2. T-014 AC2 的 timeline honesty 已满足。
3. T-014 AC2 的 lifecycle completeness 未满足。
4. 缺失的事件是：`worker_dispatched`、`review_started`。
5. 没有回填伪造历史事件。
6. T-014 的准确终态表述为：`PASSED WITH RECORDED DEVIATION`。
7. 该偏差由 Chief decision 正式接受。
8. MANUAL_PHYSICAL_GATE 仍然开放。
9. 这不是生产代码或测试失败。
10. 不影响 T-013 自动化验收结论。

Reviewer 必须在 Reviewer-owned Addendum 中确认或驳回上述结论。

---

## 7. 行号漂移的正确处理方式

不要再追加一组声称永久有效的绝对行号。

在 `Workflow一/reviews/T-012-r2.md` 追加：

`## Addendum 3 — Stable reference policy`

必须说明：

- Addendum 2 的行号仅是当时经过 `nl -ba` 验证的历史快照；
- Main 随后添加 T-014 ledger/work-log（以及未来更新），使行号发生正常漂移；
- 这不是原证据内容失效；
- 后续解析必须使用：`### Current state`、`### Task ledger` + Task ID、
  `### Work log`、具体 T-xxx closure heading、managed start/end marker；
- 如果需要调试时的行号，可临时运行 `nl -ba`，但不得将其当作长期稳定标识。

如果 `Workflow/results/T-013.md` 或 `Workflow/reviews/T-013-r1.md` 仍把旧行号
描述为"当前准确"，只允许追加简短 supersession note，不得改写历史正文。

---

## 7. Acceptance Criteria

T-015 只有满足以下全部条件才可 PASS：

1. Chief decision artifact 存在，并明确接受或拒绝 T-014 AC2 偏差。
2. 若接受偏差，T-014 被准确表述为 `PASSED WITH RECORDED DEVIATION`，
   不再表述为 7/7 全满足。
3. T-014 缺失的历史 dispatch/start 事件没有被回填或伪造。
4. T-015 自身拥有真实、完整、顺序正确的生命周期事件：
   `task_created → worker_dispatched → worker_completed → review_started →
   review_passed → task_closed`。
5. Addendum 3 使用稳定锚点策略，不再追逐永久 live 行号。
6. 所有 canonical 记录对 T-014 的结论一致。
7. `Workflow/STATE.json`、`MEMORY.md`、`events.jsonl` 一致。
8. T-000/T-013/T-014/T-015 最终状态均有明确说明。
9. `workflow_status` 聚合正确，无 READY/EXECUTING/REVIEWING 任务与 PASSED
   workflow 并存。
10. `active_agent_jobs == []`、`blockers == []`，除 MANUAL_PHYSICAL_GATE 作为
    产品验证门继续保留。
11. `git diff --check` exit 0。
12. 相对 T-015 开始快照，`Packages/**` 零新增变化。
13. 未执行 commit、push、tag、release。
14. 没有把自动化或记录 PASS 表述为真实 60/120Hz 手感 PASS。

---

## 8. Reviewer 要求

Reviewer 必须独立读取：T-015 task packet、Chief decision、
T-014 task/result/review、T-015 result、STATE、events、MEMORY、
实际任务范围 diff。逐项审核全部 Acceptance Criteria。特别检查：

- 不得因为 `state_changed` 解释了缺失事件，就把缺失事件算作已经存在；
- 不得把 Chief 接受偏差表述成原 AC2 已满足；
- T-015 自身事件必须真实完整；
- Reviewer 必须在开始审核前由 Main 记录 `review_started`；
- Reviewer 不得修改 Worker 结果或实现记录来让审核通过。

Reviewer verdict 只能是 PASS / FAIL / BLOCKED。
Reviewer 报告：`Workflow/reviews/T-015-r1.md`

---

## 9. 验证命令

至少执行并记录：

```bash
git status --short --branch
git rev-parse HEAD
git rev-parse origin/main
git diff --check
git diff --stat
python3 -m json.tool Workflow/STATE.json
python3 -m json.tool Workflow/config.json
wc -l Workflow/events.jsonl
nl -ba Workflow/events.jsonl
```

用 Python 验证：每行 JSON 可解析；sequence 严格递增且不重复；
T-015 六个生命周期事件存在且顺序正确；T-014 没有新增伪造的历史
dispatch/start；STATE 聚合状态正确。

本任务不需要重新运行 Swift test（record-only）。

---

## 10. 最终报告措辞

如果 Reviewer PASS，最终只能写：

- `T-015 RECORD ACCEPTANCE: PASS`
- `T-014: PASSED WITH RECORDED DEVIATION`
- `T-014 AC2 timeline honesty: PASS`
- `T-014 AC2 lifecycle completeness: NOT MET`
- `No historical events were fabricated`
- `T-015 lifecycle recording: COMPLETE`
- `AUTOMATED IMPLEMENTATION ACCEPTANCE: PASS`
- `MANUAL_PHYSICAL_GATE: OPEN / REQUIRED`
- `No commit / push / tag / release performed`

不得写："T-014 7/7 全部满足"、"T-014 生命周期事件齐全"、
"真机手感已验收"、"60/120Hz 已验证"。

T-015 通过后，下一项开发目标应是单独的真实设备验证任务，而不是继续修改
分页生产架构。
