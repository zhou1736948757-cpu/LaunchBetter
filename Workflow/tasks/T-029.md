# T-029 — T-027/T-028 收口、集成提交与 v0.7.5 发布

## 0. 任务性质

T-029 是 **Main-owned 的集成、提交与 v0.7.5 发布任务**。T-027 和 T-028 的实现已完成并通过独立评审（T-027 R2 PASS、T-028 PASS）。本任务**不重新修改其生产逻辑**，只负责核验、收口、提交和发布。

## 1. 验收状态（核实基线）

- **T-027** Paging Telemetry：PASSED（R2 复审 PASS + Main 独立验证 LaunchUI 522/522 无 filter 并行、69 suites）
- **T-028** Icon metadata 合并：PASSED（Reviewer PASS + 独立复现 LaunchPlatform 152/23）
- v0.7.4 已发布（80516fb）；v0.7.5 tag 未占用

## 2. 允许纳入提交的范围

1. T-027 生产代码与测试（LaunchUI：PagingPerfTelemetry.swift 新增 + 5 文件插桩 + PagingPerfTelemetryTests.swift 新增）
2. T-028 生产代码与测试（LaunchPlatform：IconContentVersionFactory.swift + IconRepositoryTests.swift）
3. T-027/T-028 正式 Workflow 记录（tasks/results/reviews/decisions/STATE/events + 相关修正）
4. v0.7.5 版本 bump（0.7.4 → 0.7.5，2 处 MARKETING_VERSION）

**不纳入**：Docs/aegis/、Workflow/evidence/、Workflow/tasks/B-REBUILD.md（隐私/机密，永不提交）；Workflow一/（历史归档只读）。

## 3. 越界检查

- T-027：不得含 feel 参数（sensitivity/axis lock/clamp/fling/spring/settle curve）、compositor gate、advanceRealClipBehindCover、第二 offset writer、容器重写、搜索/拖拽/Folder/Settings 行为改动
- T-028：不得含公开 API 变化、icon pipeline 重写、缓存 key 语义变化、无关 LaunchPlatform 改动
- 发现越界 → 停止收口，记录，不"顺便保留"

## 4. 提交结构（原子，不机械拆分）

1. T-028 独立提交（LaunchPlatform 2 文件）
2. T-027 独立提交（LaunchUI 7 文件）
3. Workflow 收口记录提交（tasks/results/reviews/decisions/STATE/events/heartbeats + T-026 结果 MINOR 修正）
4. 版本 bump 提交（0.7.4 → 0.7.5）

## 5. 发布

- tag v0.7.5（annotated 语义：轻量 tag 即可，SHA 需 origin 一致）
- push main + tag
- GitHub Release v0.7.5（Latest；沿用 v0.7.4 格式）
- verify-release.sh v0.7.5 六门全过（fresh clone）
- 发布说明事实边界：telemetry 默认关闭；不声称性能优化/真实设备验收；60Hz/120Hz 真机验证是独立后续任务

## 6. 边界声明

**已证明**：T-027/T-028 集成、测试+Release 通过、v0.7.5 tag/Release 创建、telemetry 默认关闭、session isolation+deterministic gate 自动化验证、metadata 合并测试通过。

**未证明**：比 BuhoRebuild 更丝滑、NSCollectionView 非瓶颈、telemetry 改善性能、真实触控板无卡顿、60Hz/120Hz 真实手感、cold/warm icon cache 对比、compositor activation rate 达标 —— 留给下一独立任务（真实 Release build + 真实触控板 + dense page + cold/warm cache + telemetry 采集 + 60Hz/120Hz 分开记录）。
