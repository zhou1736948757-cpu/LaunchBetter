# T-019-r1 独立评审（Reviewer）

- 日期：2026-08-27
- Reviewer：独立子代理（只读评审；红态验证在 /tmp/rev-red/ 副本进行，仓库零触碰）
- 任务：T-019 — rasterize 与 live 渲染契约对齐（名字截断 + Folder 尺寸）

## VERDICT: **PASS**

3 处修复经代码审读、CTLine 口径探针、副本红态回退复跑与 481/481 全量回归三重独立验证全部成立。评审 PASS ≠ 任务验收完成：验收 #1（真实屏幕主验收）待用户 MANUAL_PHYSICAL_GATE。

## 逐项结论（1-8）

### 1. 生产 diff 唯一性 — 一致 ✅
仅 `PageVisualRenderer.swift`（+78/−34）。T-019 增量恰为 3 处 hunk：resolveIcons `cellSize→iconSize`（:153-157）、drawCenteredText 超宽判断/截断后居中 `[.useGlyphPathBounds]→[]`（:569/:582）、drawFolderThumbnail `side=min(frame)→min(iconSize, frame.width)`（:451）。T-018 增量（canvasHeight/quartzRect/去全局翻转/阴影/渐变）与 T-018-r1 评审记录吻合，可清晰辨识。

### 2. Bug a 修复正确性 — 一致 ✅（重点项）
- 超宽判断与截断后居中共用默认 typographic bounds；截断宽度 `Double(rect.width)` 传 `CTLineCreateTruncatedLine(.end)`，同一测量口径。
- 独立探针（/tmp/rev_probe_ctline.swift）：4 组字符串 default width 逐位等于 `CTLineGetTypographicBounds`；`[.useGlyphPathBounds]` 系统性偏小（"Photoshop" 50.244 vs 51.797）；band 51pt 恰落旧口径不截断/新口径截断区间。
- 与 live 一致：AppCellView:293-294 `byTruncatingTail`+`maximumNumberOfLines=1`，labelRect 宽 cellWidth−4、10pt 全对齐。

### 3. Bug b 修复正确性 — 一致 ✅（重点项）
- (i) 请求口径 `metrics(geometry.iconSize)` == live `startFolderIconRequests` ✓
- (ii) `side = min(request.iconSize, frame.width)` == live 容器 iconPointSize 正方形 == FolderThumbnailView metrics(side: min(bounds))；与 drawAppIcon 同构 ✓
- (iii) 顶部锚定+水平居中保持 ✓
- 数学核验：side=96 → padding=10.56/gap=2.4/iconSide=23.36→23；child0 中心 (34.24,22.24) 与全部新断言一致。

### 4. R1 契约更新正当性 — 一致 ✅
29→23 数学正确；extent/中心均为新几何真实平移缩放；断言仍为有效区间（非恒真）；readPixels 翻转 CTM 与既有约定一致，非掩盖性改动。

### 5. 测试真实性 — 一致 ✅（决定性实证）
独立红态复跑（/tmp 副本回退 3 处 T-019 hunks，T-018 保留）：band 截断测试红（rightmost=212 > 204，与 Worker 逐值一致）、folder 请求测试红（pointSize=29≠23）；long/short 与 T-018 5/5 修复前后均绿。测试确实区分修复前后。

### 6. 全量回归真实性 — 一致 ✅
独立复跑：LaunchUI 481/481（66 suites）、PageVisualRendererTests 13/13、PageCompositorTests 22/22、GridIntegration 33/33、Wiring 4/4、LaunchCore 200/16、LaunchPlatform 149/23、diff --check 干净 —— 全部 exit 0 且 executed > 0。

### 7. T-013 保护条件 — 一致 ✅（零违反）
生产 diff 仅 PageVisualRenderer.swift；其余生产文件零改动；无 sleep/max(0,count) 模式；无公开 API 变化；未删任何历史测试或记录。

### 8. 合规 — 一致 ✅
HEAD == origin/main == 29e1886；无新 commit/staged/tag/push；`git diff --name-status` 全 M 无删除；事件链 seq 48-49 记录 Worker BLOCKED → Main R1/R2 决策；Release 产物存在（mtime 12:26 晚于 R2 授权 12:19）。

## 不一致/疑点（均非实质缺陷）

1. MINOR — 探针测得截断可执行边界 ≈ typo − 0.25pt（一个尾字形 advance），结果文件表述"逐位一致"略粗；不影响修复正确性。
2. MINOR — 两个 folder 几何 smoke 测试在旧代码上也通过（采样点落新旧 child0 区域交集）；判别器由 band + 请求测试承担（已红→绿实证）。
3. MINOR — PageCompositorTests +79 属 T-018 增量（非 T-019 改），T-019 未触该文件。

## 一句话总结

T-019 三重独立验证全部成立，生产 diff 唯一、T-013 零违反、无 commit/push/tag —— **PASS**（真实屏幕主验收待用户，MANUAL_PHYSICAL_GATE 保持 OPEN 属任务包预期）。
