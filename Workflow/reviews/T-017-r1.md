# T-017-r1 独立评审（Reviewer）

- 日期：2026-08-27
- Reviewer：独立子代理（与 Worker 完全隔离，只读评审）
- 任务：T-017 修复 PageCompositor 镜像 bug（方案 A：移除 isGeometryFlipped）

## VERDICT: **PASS**

证据链完整、可独立复现，无实质缺陷。评审 PASS ≠ 任务验收完成：验收 #3（交互验证）待用户执行，`MANUAL_PHYSICAL_GATE` 保持 OPEN/REQUIRED。

## 逐项结论

### 1. 生产 diff 唯一性 — 一致 ✅
`git diff -- Packages/LaunchUI/Sources/` + `git diff --name-status origin/main` + 全生产源码 grep：生产 diff 仅 `PageCompositor.swift` 恰好 1 行删除（`layer.isGeometryFlipped = true`，原 :191）；`isGeometryFlipped` 全生产源码 **0 残留**；无其他生产文件被改。

### 2. 方向测试真实性 — 一致 ✅
- 走**真实 activate 路径**：`compositor.activate(placements:pageWidth:startOffset:hostLayer:liveLayer:)` + 真实 `rasterize` 产物（140×60，2 列 1 行，item0 红色图标），非裸 CALayer 测试。
- 像素断言确实区分翻转：顶部 (30,15) 红图标 + 底部行 39..53 白标签 + 底部无红，三断言在镜像态全部失败。
- `live.opacity == 0`、`layerFramesForDiag == [placement.baseFrame]`、`host.sublayers?.count == 1` 均在。
- **hostLayer.render(in:) 不应用子层 flip —— Reviewer 独立实验证实成立**：host 树渲染在子层 flipped=true/false 下输出完全相同；直接渲染子层时 flip 生效。测试改直接渲染 compositor 子层是唯一能复现镜像机制的路径。
- 几何自洽：drawAppIcon/drawLabel/slotOrigin 与扫描区吻合。

### 3. TDD 红→绿证据 — 一致 ✅（最强验证）
Reviewer 用 git worktree 在 HEAD（修复前）检出 + 复制新测试独立复跑红态：**恰好 3 断言失败**（顶部非红、无标签、底部有红 = 红蓝颠倒），失败消息与 `Workflow/results/T-017.md` §2.3 记录**逐字一致**，exit 1；绿态（当前代码）复跑 1/1 通过 exit 0。假阳性捕获故事（host 树渲染版修复前即通过）与机制结论吻合，非"测试永远通过"。

### 4. 全量回归真实性 — 一致 ✅
Reviewer 完整复跑全部 6 条命令：PageCompositorTests **21/21**、GridIntegration **33/33**、LaunchUI 全量 **471/66**、LaunchCore **200/16**、LaunchPlatform **149/23**、`git diff --check` 干净 —— 全部 exit 0 且 executed > 0，与 Worker 声称逐项吻合。

### 5. T-013 保护条件 — 一致 ✅（零违反）
生产改动仅 1 行私有 layer 属性赋值删除；activate 签名、参数、默认值、compositor 激活条件、公开 API 均未动；`Workflow一/` legacy backup 完好；无 sleep/动画时长/`max(0,count)` 类改动。

### 6. 合规 — 一致 ✅
HEAD == origin/main == cb180e4（基线）；reflog 无新 commit；无新 tag；无 push/release 痕迹；fsck dangling commits 均为 T-017 之前历史遗留；事件链 decision_recorded→task_created→worker_dispatched→worker_completed→review_started 完整且时间戳先于实际调度。

## 不一致/疑点

无实质不一致。2 个 MINOR（不影响判定）：
1. T-016 遗留 `calayer-flip-experiment.swift` 注释"测试图: 上半红、下半蓝"不准确（NSImage lockFocus 为 y-up，图像实为上蓝下红）；经验结论正确且被独立实验重新证实 —— 属 T-016 既有工件，非 T-017 引入。
2. 验收 #3（交互验证）标记"待用户"且 `MANUAL_PHYSICAL_GATE` 保持 OPEN/REQUIRED，符合任务包范围界定，非缺陷。

## 一句话总结

生产改动唯一（1 行删除）、方向测试走真实 activate 路径且像素断言经独立实验与红态 worktree 复跑双重证实、全量回归实跑全绿、T-013 零违反、无 commit/push/tag/release —— **PASS**。
