# T-020-r1 独立评审（Reviewer）

- 日期：2026-08-27
- Reviewer：独立子代理（只读评审；红→绿验证用 stash 独立复跑）
- 任务：T-020 — Folder 背景视觉对齐（移除动态毛玻璃，统一静态 glass surface）

## VERDICT: **PASS**

生产 diff 严格限定 2 文件、NSVisualEffectView→NSView 转换与常量源（0.08/0.38/0.18/0.02）逐项符合 Chief 修正版定义（去毛玻璃、非加 blur），红→绿与全量回归均独立复现，T-013 零违反，合规无 commit/push/tag。评审 PASS ≠ 任务验收完成：真实屏幕主验收待用户 MANUAL_PHYSICAL_GATE。

## 逐项结论（1-7）

### 1. 生产 diff 唯一性 — 一致 ✅
仅 AppCellView.swift（+31/−13）、PageVisualRenderer.swift（+9/−7）。HEAD=9e7c768 已含 T-018/T-019，working tree diff vs HEAD 即纯 T-020 增量。

### 2. NSView 转换正确性 — 一致 ✅
`FolderThumbnailView: NSView`（:698）；material/blendingMode/state 已删；iconContainerLayer/sheenLayer/iconLayers×9/corner radius/border/background 全部保留（通读 670-848 逐项核对）；NSVisualEffectView 仅存注释；CIFilter/CABackdropLayer/material/blendingMode/filters 零命中。

### 3. 常量源正确性 — 一致 ✅
FolderThumbnailStyle（internal）= 0.08/0.38/0.18/0.02，与修复前逐字一致；live/raster 引用同一常量；硬编码 grep 仅命中常量定义；无协议/策略抽象。

### 4. 测试真实性 — 一致 ✅
4 测试均非恒真（常量快照、结构断言、layer 颜色比对 1e-6、raster 像素采样合成 alpha 推导）；红→绿独立复跑：stash 旧查找器 → wiring 1 红 + lazy 6 红（exit 1）→ 恢复全绿，与 Worker 完全一致。

### 5. 全量回归真实性 — 一致 ✅
LaunchUI 485/485（67 suites）、LaunchCore 200/16、LaunchPlatform 149/23、diff --check 干净；独立 Release 重建（t020-derived-review）BUILD SUCCEEDED。

### 6. T-013 保护条件 — 一致 ✅
受保护文件 vs HEAD 零 diff；无 sleep/max(0,count)；无公开 API 变化（FolderThumbnailView 仍 private，FolderThumbnailStyle 新增 internal）；历史记录原样保留。

### 7. 合规 — 一致 ✅
HEAD 仍 9e7c768、未 push、无新 tag、reflog 无 T-020 提交、无历史删除。

## 不一致/疑点（均 MINOR）

1. Worker 报告 diff 行数（+44/−12 等）与实测（+31/−13）不符 —— 总变更行数误标为 +/− 拆分，纯数字笔误。
2. 真实屏幕验收待用户手动确认（Worker 已诚实标注 MANUAL_PHYSICAL_GATE）。

## 一句话总结

T-020 符合 Chief 修正版定义（去毛玻璃、非加 blur），三重独立验证成立 —— **PASS**（真实屏幕主验收待用户）。
