# T-020 — Folder 背景视觉对齐（移除动态毛玻璃，统一静态 glass surface）

- 任务包：`Workflow/tasks/T-020.md`
- Worker 完成时间：2026-08-27（本地）
- 工作流：chef-worker-reviewer-workflow v1.4
- **Status: COMPLETED**（实现 + 测试 + 全量回归 + Release 重建全部完成；§7 真实屏幕验收待用户手动确认）

---

## 1. 根因复核（任务包 §2 逐条）

| 项 | 复核结果 |
|---|---|
| live 层 | `AppCellView.swift:679` `FolderThumbnailView: NSVisualEffectView`，init 设 `material = .hudWindow`（:688）、`blendingMode = .withinWindow`、`state = .active` —— **动态系统毛玻璃** ✅ 与任务包一致 |
| raster 层 | `PageVisualRenderer.swift:444` `drawFolderThumbnail` 静态绘制：white fill 0.08 + border 0.38 + sheen 0.18→0.02 —— **无系统 blur/material** ✅ 与任务包一致 |
| 结论 | 任务包根因成立：live 动态毛玻璃 vs raster 静态绘制 → 滑动时（compositor 合成层替换 live）毛玻璃消失，视觉不可能一致 |

## 2. 修复 diff（Chief 修正版，严格按任务包 §3）

### 2.1 生产改动（唯一生产文件 2 个：`AppCellView.swift` + `PageVisualRenderer.swift`）

**`AppCellView.swift`（+44/−12）**

```diff
-/// 主网格文件夹缩略图: AppKit 磨砂背景承载最多 3x3 个真实图标。
+/// 文件夹缩略图表面样式常量 —— live(FolderThumbnailView)与
+/// raster(PageVisualRenderer.drawFolderThumbnail)共用的唯一数值源。
+enum FolderThumbnailStyle {
+    static let backgroundAlpha: CGFloat = 0.08
+    static let borderAlpha: CGFloat = 0.38
+    static let sheenTopAlpha: CGFloat = 0.18
+    static let sheenBottomAlpha: CGFloat = 0.02
+}
+
+/// 主网格文件夹缩略图: 静态半透明 glass surface 承载最多 3x3 个真实图标。
-private final class FolderThumbnailView: NSVisualEffectView {
+private final class FolderThumbnailView: NSView {
     override init(frame frameRect: NSRect) {
         super.init(frame: frameRect)
-        material = .hudWindow
-        blendingMode = .withinWindow
-        state = .active
         wantsLayer = true
         iconContainerLayer.masksToBounds = true
         sheenLayer.colors = [
-            NSColor.white.withAlphaComponent(0.18).cgColor,
-            NSColor.white.withAlphaComponent(0.02).cgColor,
+            NSColor.white.withAlphaComponent(FolderThumbnailStyle.sheenTopAlpha).cgColor,
+            NSColor.white.withAlphaComponent(FolderThumbnailStyle.sheenBottomAlpha).cgColor,
         ]
         ...
-            layer.backgroundColor = NSColor.white.withAlphaComponent(0.08).cgColor
-            layer.borderColor = NSColor.white.withAlphaComponent(0.38).cgColor
+            layer.backgroundColor = NSColor.white
+                .withAlphaComponent(FolderThumbnailStyle.backgroundAlpha).cgColor
+            layer.borderColor = NSColor.white
+                .withAlphaComponent(FolderThumbnailStyle.borderAlpha).cgColor
```

- **保留**：iconContainerLayer、sheenLayer、iconLayers × 9、corner radius（`updateLayout` 用 `FolderThumbnailMetrics`）、border（1/backingScale）、background —— 逐项核对无删改。
- **删除**：material、blendingMode、state（3 行）。
- **禁止项零引入**：无 CIFilter blur、无 CABackdropLayer、无 private CoreAnimation filters、无 background screenshot blur（grep 复核：`CIFilter|CABackdropLayer` 在 AppCellView.swift 零命中）。
- 常量源为 LaunchUI-internal `enum FolderThumbnailStyle`（无协议/策略抽象），数值与修复前完全一致。

**`PageVisualRenderer.swift`（+16/−14，仅 drawFolderThumbnail 常量引用）**

```diff
-        // 背景: 白 0.08 + 边框白 0.38(1/scale)。
-        context.setFillColor(red: 1, green: 1, blue: 1, alpha: 0.08)
+        context.setFillColor(red: 1, green: 1, blue: 1, alpha: FolderThumbnailStyle.backgroundAlpha)
         context.fill(quartz)
-        context.setStrokeColor(red: 1, green: 1, blue: 1, alpha: 0.38)
+        context.setStrokeColor(red: 1, green: 1, blue: 1, alpha: FolderThumbnailStyle.borderAlpha)
         ...
-                CGColor(red: 1, green: 1, blue: 1, alpha: 0.18),
-                CGColor(red: 1, green: 1, blue: 1, alpha: 0.02),
+                CGColor(red: 1, green: 1, blue: 1, alpha: FolderThumbnailStyle.sheenTopAlpha),
+                CGColor(red: 1, green: 1, blue: 1, alpha: FolderThumbnailStyle.sheenBottomAlpha),
```

- 硬编码残留复核：`grep '0\.08|0\.38|0\.18|0\.02'` 在两个生产文件中仅命中 `FolderThumbnailStyle` 常量定义本身（4 行），绘制路径零硬编码。

### 2.2 测试改动（允许清单内）

**`FolderThumbnailMetricsWiringTests.swift`（+16/−6）**：`findFolderThumbnailView`/`iconLayers(of:)` 由 `NSVisualEffectView` 类型查找改为**结构识别**（layer → iconContainerLayer → sheen CAGradientLayer；不耦合私有类型名，对旧/新实现均成立）。断言本体零改动。

**`AppCellFolderThumbnailLazyAllocationTests.swift`（+19/−7）**：`folderThumbnailViews(in:)` 同结构识别改造；套件头注释同步。

**`FolderThumbnailStyleConsistencyTests.swift`（新增，4 测试）**：

| 测试 | 内容 |
|---|---|
| `constantsMatchPreFixValues` | 常量 == 0.08/0.38/0.18/0.02（修复前数值） |
| `liveThumbnailIsPlainNSView` | 结构断言：缩略图是普通 NSView、**非** NSVisualEffectView（T-020 核心） |
| `liveLayerColorsUseStyleConstants` | live layer 背景/border/sheen 颜色 alpha == 常量（接线） |
| `rasterizedFolderSurfaceMatchesStyleConstants` | raster 合成 alpha == 常量推导值（背景 0.08 + sheen 渐变合成，顶/中/底三点 + 单调性）—— 证明 raster 引用同一常量源 |

## 3. 测试红→绿（真实执行）

### 3.1 RED —— 旧测试查找器 vs 新生产代码（类型变更破坏旧断言）

```bash
# 临时 stash 两个适配测试文件(还原 HEAD 的 is NSVisualEffectView 查找器), 生产代码保持新实现
swift test --package-path Packages/LaunchUI --filter FolderThumbnailMetricsWiringTests
# ✘ "live: 子图标 layer frame = metrics.childFrame..." findFolderThumbnailView → nil
# ✘ Test run with 4 tests in 1 suite failed with 1 issue, exit 1
swift test --package-path Packages/LaunchUI --filter AppCellFolderThumbnailLazyAllocationTests
# ✘ 6/6 失败(12 issues): folderThumbnailViews 恒为空 → count==1/isHidden 断言全红
# ✘ Test run with 6 tests in 1 suite failed with 12 issues, exit 1
# (stash pop 恢复新查找器)
```

- 新测试文件对旧生产代码**不可编译**（`FolderThumbnailStyle` 不存在）—— 常量源为新增符号，旧代码侧整体红；核心断言 `!(thumbnail is NSVisualEffectView)` 对旧实现（类本身是 NSVisualEffectView）按构造为红。

### 3.2 GREEN —— 新实现 + 新测试

```bash
swift test --package-path Packages/LaunchUI --filter FolderThumbnailStyleConsistencyTests
# ✔ 4/4 全绿, exit 0
swift test --package-path Packages/LaunchUI --filter FolderThumbnailMetricsWiringTests
# ✔ 4/4 全绿, exit 0
swift test --package-path Packages/LaunchUI --filter AppCellFolderThumbnailLazyAllocationTests
# ✔ 6/6 全绿, exit 0
```

- 开发期一次真实红→绿：raster 像素测试首版采样行映射错误（buffer row 0 = 图像底部，探针实证）→ 修正映射后绿；探针测试已删除，不留污染。

## 4. 全量回归（真实执行, exit/executed/passed/failed）

| # | 命令 | exit | executed | passed | failed | 说明 |
|---|---|---|---|---|---|---|
| 1 | `swift test --package-path Packages/LaunchUI --filter PageVisualRendererTests` | 0 | 13 | 13 | 0 | |
| 2 | `swift test --package-path Packages/LaunchUI --filter PageCompositorTests` | 0 | 22 | 22 | 0 | |
| 3 | `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` | 0 | **33** | **33** | **0** | |
| 4 | `swift test --package-path Packages/LaunchUI` | 0 | **485** | **485** | **0** | 67 suites（481 基线 + 4 T-020 新增） |
| 5 | `swift test --package-path Packages/LaunchCore` | 0 | **200** | **200** | **0** | 200/16 |
| 6 | `swift test --package-path Packages/LaunchPlatform` | 0 | **149** | **149** | **0** | 149/23 |
| 7 | `git diff --check` | 0 | — | — | — | 无空白错误 |

- 无 `exit 0 且 executed == 0`（每项 executed > 0）。
- SwiftPM 缓存未报错；按任务包建议全程加 `CLANG_MODULE_CACHE_PATH=/private/tmp SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp` 前缀。

## 5. Release 重建（任务包 §6）

```bash
xcodebuild -project LaunchBetter.xcodeproj -scheme LaunchBetter -configuration Release \
  -derivedDataPath /tmp/launchbetter-t020-derived build
# ** BUILD SUCCEEDED **, exit 0(2026-08-27)
# 产物: /tmp/launchbetter-t020-derived/Build/Products/Release/LaunchBetter.app
```

## 6. 验收对照（任务 §7 逐条）

| # | 验收标准 | 状态 | 说明 |
|---|---|---|---|
| 1 | live/raster 几何一致（FolderThumbnailMetrics 仍为唯一几何源） | ✅ | 未触碰 FolderThumbnailMetrics；`updateLayout`/`drawFolderThumbnail` 几何路径零改动；wiring 4/4 绿 |
| 2 | background alpha 一致（0.08） | ✅ | 常量源 `backgroundAlpha = 0.08`；live layer 颜色接线测试 + raster 合成像素测试双证 |
| 3 | border alpha 一致（0.38） | ✅ | 常量源 `borderAlpha = 0.38`；live 接线测试证 |
| 4 | sheen stops 一致（0.18→0.02） | ✅ | 常量源 `sheenTopAlpha/sheenBottomAlpha`；live 接线 + raster 顶/底像素证 |
| 5 | child icon 几何一致 | ✅ | 未触碰；wiring 4/4 + GridIntegration 33/33 绿 |
| 6 | 常量源被 live/raster 共用（无硬编码残留） | ✅ | 两生产文件仅引用 `FolderThumbnailStyle`；grep 复核零硬编码残留 |
| 7 | 真实屏幕: 静止/滑动背景无亮度材质跳变、尺寸/图标无跳变 | **代码侧完成**（待用户 MANUAL_PHYSICAL_GATE） | live 已无动态毛玻璃，与 raster 同为静态 0.08/0.38/0.18→0.02 玻璃面；请运行 Release app 实际确认 |

### 附加收益（不声称）
- 普通 CALayer surface 可能略降 GPU/compositing 成本 —— **未测，不声称**（任务包 §7 附加收益条款）。

## 7. T-013 保护条件逐条确认（任务包 §8, 零违反）

| 保护条件 | 确认 |
|---|---|
| PagingInteractionController 保持唯一 paging 运动写入者 | ✅ 未触碰 |
| PageCompositor 仅 presentation | ✅ 未触碰 |
| 不改 spring/stiffness/damping/fling 阈值/rubber-band/settle 时长/默认 linear 1.3/normalizedDamped 默认/compositor 激活条件 | ✅ 未触碰 |
| 不改 100ms prepare debounce | ✅ 未触碰 |
| 不改 advanceRealClipBehindCover() | ✅ 未触碰 |
| 不用 sleep 掩盖 | ✅ 无 sleep 引入 |
| 不用 max(0,count) 掩盖 | ✅ 无 |
| 不改公开 API | ✅ 仅改 private 类 FolderThumbnailView 基类 + 新增 internal enum FolderThumbnailStyle + private 方法内部实现；无公开签名/可见性变化 |
| 不删历史 FAIL 记录 | ✅ T-017/T-018/T-019 测试与记录原样保留 |
| 不 commit/push/tag/release | ✅ 工作树未暂存未提交（git status 确认） |

## 8. 输出物清单

- `Packages/LaunchUI/Sources/LaunchUI/AppCellView.swift`（NSVisualEffectView→NSView + FolderThumbnailStyle 常量源 + 常量引用）
- `Packages/LaunchUI/Sources/LaunchUI/PageVisualRenderer.swift`（drawFolderThumbnail 常量引用，仅此）
- `Packages/LaunchUI/Tests/LaunchUITests/FolderThumbnailStyleConsistencyTests.swift`（新增 4 测试）
- `Packages/LaunchUI/Tests/LaunchUITests/FolderThumbnailMetricsWiringTests.swift`（查找器结构识别改造）
- `Packages/LaunchUI/Tests/LaunchUITests/AppCellFolderThumbnailLazyAllocationTests.swift`（查找器结构识别改造）
- `Workflow/results/T-020.md`（本文件）
- Release app: `/tmp/launchbetter-t020-derived/Build/Products/Release/LaunchBetter.app`

## 9. 遗留与不确定性（诚实记录）

1. **MANUAL_PHYSICAL_GATE**: 真实屏幕验收（§6 #7 主验收）须用户手动确认；代码侧证据链完整（红→绿 + 全量 485/485 + BUILD SUCCEEDED）。
2. raster 像素测试阈值基于合成 alpha 推导（±3-4 个 8bit 级），对字体/渲染后端不敏感（采样点避开文字/边框/圆角）；若未来 sheen 渐变实现变化需同步推导。
3. 结构识别查找器（layer 树含 CAGradientLayer）依赖缩略图 layer 结构；若未来重构 layer 树需同步测试查找器（与旧类型查找器同等的耦合度，但不再耦合 AppKit 类型）。
