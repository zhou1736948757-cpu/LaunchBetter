# T-022 — 长名字截断宽度契约对齐（raster 截断宽度 = labelRect.width − 7.5）

- 日期：2026-08-27
- 角色：Worker（T-022，EXECUTING）
- 基线：`HEAD == 9e7c768`（v0.7.1）；working tree 含 T-020 未提交改动（AppCellView.swift + PageVisualRenderer.swift 常量引用，与 label 路径无关）
- 方案：Chief 裁定采纳"常量 7.5"方案，拒绝"运行时实测"方案；方向 = raster 对齐 live，**不改 live**

## 1. 改动文件与 diff

### 生产代码：`Packages/LaunchUI/Sources/LaunchUI/PageVisualRenderer.swift`（仅截断宽度一处）

| 位置 | 改动 |
|---|---|
| :549 | 新增命名常量 `nonisolated private static let labelTruncationInset: CGFloat = 7.5`，注释完整：来源（NSTextFieldCell 私有绘制内缩，实测 7.5–7.7pt，drawingRect/titleRect 均报全宽不可查询，T-021 probe4/5/8）、选择依据（probe6 实证 raster @96.5 截断 → 94.0pt 居中 ≈ live 93.5pt，0.5pt 内对齐）、依赖前提（label font 10pt 硬编码）、macOS 大版本升级需用 Workflow/evidence/T-021/ 的 probe4/5/8 重新测量、字体可配置时须并入 PageVisualKey |
| :574 | `drawLabel` 传 `truncationWidth: labelRect.width - Self.labelTruncationInset`（≈ cellSize − 11.5）；**labelRect 与居中不变** |
| :585 | `drawCenteredText` 新增显式参数 `truncationWidth: CGFloat? = nil`（默认 rect.width），避免把 NSTextFieldCell 特定内缩耦合进通用文本原语 |
| :608-613 | 截断决策与 `CTLineCreateTruncatedLine` 宽度均用 `effectiveTruncationWidth = truncationWidth ?? rect.width` |

- 占位字母路径（truncatingTail: false）不受影响（不传 truncationWidth，走默认 rect.width，且不截断）
- 文件夹标签走同一 `drawLabel` 自动同修（无额外改动）
- 缓存键：**未改**（截断宽度 = cellSize−11.5 是 cellSize 的纯函数，cellSize 已在 geometry 签名中；常量注释已注明字体可配置时须并入 key）

### 测试：`Packages/LaunchUI/Tests/LaunchUITests/PageVisualRendererTests.swift`（+101 行）

| 位置 | 新增 |
|---|---|
| :503 | `typoWidth(_:)` 辅助：CTLineGetBoundsWithOptions 实测 typographic 宽（与渲染器同口径） |
| :513 | `labelInkExtent(_:width:height:cellSize:scale:)` 辅助：标签带内白像素 ink 范围（minX, maxX） |
| :537 | `truncationBoundaryHitShowsEllipsis`（T-022 边界命中） |
| :570 | `truncationUnderTruncationProtection`（T-022 欠截断保护） |

### 未改动（T-013 保护）
`AppCellView.swift`（live 路径）、`PageCompositor.swift`、PageVisualKey/缓存签名、其他生产/测试文件。未 commit/push/tag/release。

## 2. 测试红→绿记录（真实执行）

### 红态（修复前，仅测试已加）
```
✘ Test "T-022 边界命中: typo 宽 ∈ (96.5, 104] 的名字 → 省略号(修复前全名, 红→绿)"
   recorded an issue at PageVisualRendererTests.swift:563:9:
   Expectation failed: (widthPt → 97.75) <= 95.0
✘ Test run with 15 tests in 1 suite failed after 0.048 seconds with 1 issue.
EXIT=1
```
- 边界命中测试**红**：修复前截断宽度 = labelRect.width = 104，"Affinity Designer 2.6"（98.72pt）≤ 104 → 全名绘制，ink 宽 97.75pt > 95.0 阈值
- 欠截断保护测试**绿**（保护性测试，修复前后均绿）："GarageBand 10.4.12"（95.77pt）完整绘制，ink 宽 94.25pt ≥ 93.0
- T-019 三个截断测试红态下全绿

### 绿态（修复后）
```
✔ Test run with 15 tests in 1 suite passed after 0.045 seconds.
EXIT=0
```
- 边界命中：截断宽度 = 96.5 → 98.72 > 96.5 → 省略号，ink 宽 91.25pt ≤ 95.0 ✓
- 欠截断保护：95.77 ≤ 96.5 → 完整绘制 ✓
- T-019 三测试保持绿 ✓

### 阈值校准依据（确定性像素实测，非目测）
- 全名 "Affinity Designer 2.6" ink 宽 391px@4x = 97.75pt；截断@96.5 后 365px = 91.25pt → 阈值 95.0pt（余量 11/15px）
- 全名 "GarageBand 10.4.12" ink 宽 377px = 94.25pt；过截断（内缩 ≥ 9.75）≈ 368px = 92.0pt → 阈值 93.0pt
- 用 ink 宽度而非单边 rightmost：全名右缘被 AA 削到 102.25pt，与截断行右缘理论上限重合，单边不可分（首版测试因此误绿，已修正）

## 3. 全量回归表（真实执行）

| 命令 | exit code | executed | passed | failed |
|---|---|---|---|---|
| `swift test --package-path Packages/LaunchUI --filter PageVisualRendererTests` | 0 | 15 | 15 | 0 |
| `swift test --package-path Packages/LaunchUI --filter PageCompositorTests` | 0 | 22 | 22 | 0 |
| `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` | 0 | 33 | 33 | 0 |
| `swift test --package-path Packages/LaunchUI` | 0 | 487（67 suites） | 487 | 0 |
| `swift test --package-path Packages/LaunchCore` | 0 | 200（16 suites） | 200 | 0 |
| `swift test --package-path Packages/LaunchPlatform` | 0 | 149（23 suites） | 149 | 0 |
| `git diff --check` | 0 | — | clean | — |

- LaunchUI 487 = 485 基线 + 2 新增（T-022）；无 exit 0 且 0 测试
- SwiftPM 缓存：`CLANG_MODULE_CACHE_PATH=/private/tmp SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp`

## 4. Release 重建

```bash
xcodebuild -project LaunchBetter.xcodeproj -scheme LaunchBetter -configuration Release -derivedDataPath /tmp/launchbetter-t022-derived build
```
→ **BUILD SUCCEEDED**（exit 0）；产物 `/tmp/launchbetter-t022-derived/Build/Products/Release/LaunchBetter.app`

## 5. PNG 证据（Workflow/evidence/T-022/）

生成方式：真实 `PageVisualRenderer.rasterize`（默认几何 cellSize=108/iconSize=80，2x），ImageIO 存 PNG；Python 像素解析标签带（y-down 88..101 → 2x 图像行 176..202，白像素 a>40 && r,g,b>200）。before = v0.7.1 源码（T-021 证据副本），after = 修复后源码。

| PNG | 修复前 | 修复后 | 判定 |
|---|---|---|---|
| `pagevisual_affinity_2x.png`（"Affinity Designer 2.6" 98.72pt） | 98.0pt = 全名（无省略号） | **91.5pt = 省略号** | 全名→省略号 ✓ |
| `pagevisual_longname_2x.png`（"Adobe Photoshop 2026" 113.8pt） | 100.5pt | **94.0pt** | 100.5→94.0，命中 T-021 预测 ~93.5-94pt ✓ |

- 文件：`pagevisual_affinity_2x.png`（after）、`pagevisual_affinity_2x_before.png`、`pagevisual_longname_2x.png`（after）、`pagevisual_longname_2x_before.png`
- 修复后 affinity 的 91.5pt 与 live 对照（T-021 `live_affinity_2x.png` 88.5pt）同量级（raster 含完整省略号行宽，live 另有私有内缩，方向一致）
- 探针源码：/tmp/T022/（measure6/measure7 为阈值校准的确定性像素实测）

## 6. 验收对照（任务 §6 逐条）

1. **新增回归测试** ✓：边界命中（红→绿，实测记录见 §2）+ 欠截断保护 + T-019 三测试保持绿
2. **真实屏幕（主验收，用户手动）** ⏳ 待用户：compositor ON 滑动中/停稳后带内名字均省略号、无"滑动见全名"闪现；`--disable-pagecompositor` 静止态截断一致；实测前需重启 app（内存 cache 无旧代）。本任务已交付修复后 Release app（§4）
3. **全量回归** ✓：LaunchUI 487/487、LaunchCore 200/16、LaunchPlatform 149/23、GridIntegration 33/33，exit 0、executed > 0（§3）
4. **T-013 保护条件零违反** ✓：生产 diff 仅 PageVisualRenderer.swift 截断宽度一处（§1）；live 路径/compositor/缓存键/公开 API 零改动（§7 逐条）
5. **证据链** ✓：修复前后 PNG 对比（§5）、测试红→绿记录（§2）、真实屏幕确认待用户（§6.2）

## 7. T-013 保护条件逐条确认

| 条件 | 状态 |
|---|---|
| PagingInteractionController 保持唯一 paging 运动写入者 | ✓ 未触碰 |
| PageCompositor 仅 presentation | ✓ 未触碰 |
| 不改 spring/stiffness/damping/fling/rubber-band/settle 时长/默认 linear 1.3/normalizedDamped 默认/compositor 激活条件 | ✓ 未触碰 |
| 不改 100ms prepare debounce | ✓ 未触碰 |
| 不改 advanceRealClipBehindCover() | ✓ 未触碰 |
| 不得 sleep 掩盖 | ✓ 无 |
| 不得 max(0,count) 掩盖 | ✓ 无 |
| 不得改公开 API | ✓ 仅新增 private 参数（默认 nil，行为不变）与 private 常量 |
| 不得删历史 FAIL 记录 | ✓ 未删 |
| 不得 commit/push/tag/release | ✓ 未执行（需 Main 确认） |

## 8. 偏差与说明

- 首版边界测试用 rightmost 单边阈值（102.5pt）在修复前误绿：全名右缘被 AA 削至 102.25pt，与截断行右缘理论上限重合。已改为 ink 宽度指标（阈值 95.0pt），红→绿成立（§2 校准依据）
- T-021 报告 affinity before 为 100.5pt，本任务同图实测 98.0pt：差异来自分析行范围/阈值口径（T-021 探针脚本行范围公式与实测不符，其结论以 Python 分析为准）；本任务 before/after 用同一脚本同一口径，对比有效
- 未提交任何 git 操作；working tree 保持 T-020 既有改动 + 本任务改动

## 9. Reviewer 关注点

- 生产 diff 唯一性：仅 PageVisualRenderer.swift 截断宽度一处（§1 行号可查）
- 常量 7.5 正确性：注释完整（来源/依据/依赖前提/重测指引）、显式参数、labelRect/居中不变
- 新增测试真实性：边界命中红→绿（§2 原始输出）、欠截断保护、T-019 三测试保持绿
- 全量回归真实性：§3 逐命令 exit code + 计数
- T-013 零违反：§7 逐条

---

## 验收注记（2026-08-27，Main 追加，Chief 决策）

- **验收 #1"省略号"条款勘误**：该条款基于 T-021 错误假设（"恒附省略号"）。T-022 宽度修复本身正确（PASSED），但 raster 截断实为硬切（"Affinity Designer 2." 无省略号字形）。
- **省略号对齐由 T-023 交付**（显式 "…" token，7.5 常量不变）。
- T-022 真实屏幕验收（§6.2）在 T-023 完成后一并执行。
