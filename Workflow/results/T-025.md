# T-025 — searchQuery 移出 LauncherStore（displayRevision 语义收敛）

- 日期：2026-08-27
- 角色：Worker（T-025，EXECUTING）
- 基线：`HEAD == origin/main == 190c604`（v0.7.3）
- 任务包：`Workflow/tasks/T-025.md`（§2 方案严格按此执行）

## 1. Status

**PASS** — 实现 + 测试红→绿 + 全量回归 + Release 重建全部完成。生产 diff
仅限搜索相关文件（LauncherStoring/LauncherStore/GridViewController/
LauncherWindowController + 诊断探针），PageCompositor/PagingInteractionController/
PageVisual 渲染路径零改动（T-013 保护条件零违反）。未 commit/push/tag/release。

## 2. 改动文件与行（生产 diff）

### 2.1 生产代码（5 个文件，+81/−70 含既有脏工作树；本任务净改动见下）

| 文件 | 改动 |
|---|---|
| `Packages/LaunchUI/Sources/LaunchUI/LauncherStoring.swift` | 移除 `var searchQuery: String { get set }`（原 36-37 行）；`searchResults()` → `searchResults(for query: String)`（原 60-61 行）；`displayRevision` 文档注释移除"搜索"（原 54 行） |
| `LaunchBetterApp/LauncherStore.swift` | 移除 `public var searchQuery` didSet bumpRevision（原 88-95 行）；`searchResults()` → `searchResults(for query: String)`（原 414-418 行，trim 逻辑不变）；`displayRevision` 文档注释移除"搜索"（原 103 行） |
| `Packages/LaunchUI/Sources/LaunchUI/GridViewController.swift` | 新增 `var searchQuery = ""`（UI 层持有，296 行附近）+ `private var lastAppliedSearchQuery = ""`（344 行附近）；`refresh()` 改为 `revisionChanged \|\| searchChanged` 双信号（原 679-700 行）；`applyLatestData()` 用 `store.searchResults(for: searchQuery)`（原 711 行）；刷新注释移除"搜索"（原 664 行） |
| `Packages/LaunchUI/Sources/LaunchUI/LauncherWindowController.swift` | 类注释（271 行）；`hide()` 清空改 `gridViewController.searchQuery = ""`（原 1119 行）；`launchFirstSearchResult()` 用 `store.searchResults(for: gridViewController.searchQuery)`（原 1625 行）；`controlTextDidChange` 写 `gridViewController.searchQuery`（原 1634 行）；新增诊断 seam `diagnosticSetSearchQuery(_:)`（1434 行附近） |
| `LaunchBetterApp/Diagnostics/*.swift`（5 个探针） | SearchProbe/SmokeProbe/SourcesProbe/AppLibraryShotProbe/DiagnosticRunner：`store.searchQuery = X` → `windowController.diagnosticSetSearchQuery(X)`；`store.searchResults()` → `store.searchResults(for: X)` |

### 2.2 测试文件（19 个）

- 新增 `Packages/LaunchUI/Tests/LaunchUITests/SearchRevisionSemanticsTests.swift`（2 测试 + 1 suite）
- 18 个既有测试替身：移除 `var searchQuery = ""`；`searchResults()` → `searchResults(for query:)`；带 `searchResultsValue` 的替身改为 query-aware（空/空白 query → nil）
- 3 个搜索驱动测试补 `grid.searchQuery = "photo" / ""`（SearchPageRestoreTests、AppLibrarySurfaceNavigationTests、PageCompositorGridIntegrationTests）

### 2.3 设计要点（§2 方案逐条）

1. `LauncherStoring` 移除 `searchQuery`（get set）✓
2. `LauncherStore` 保有 `searchIndex`，不保有 query ✓（`searchResults(for:)` 按传入 query 过滤，trim 语义与修复前一致）
3. 新增 `func searchResults(for query: String) -> [DisplayModel.DisplayItem]?` ✓（以现有调用方为准：GridViewController/LauncherWindowController/探针）
4. `displayRevision` 不再因搜索递增 ✓（didSet bumpRevision 整体移除）；搜索不使普通 PageVisual cache identity 失效 ✓（PageVisualKey 含 displayRevision，修订不再被搜索污染）
5. 文档注释同步修正 ✓（LauncherStoring/LauncherStore/GridViewController 三处"搜索"移除）
6. 搜索过滤后的 grid 显示逻辑行为不变 ✓（`applyLatestData()` 的 enterSearchMode/exitSearchMode/applyDisplayModel/页面恢复分支零改动；`refresh()` 仅把"搜索变化"从 revision 信号拆为独立 `searchChanged` 信号，触发路径相同）

## 3. 测试红→绿

### 3.1 红（修复前）

```
$ swift test --package-path Packages/LaunchUI --filter SearchRevisionSemanticsTests
.../SearchRevisionSemanticsTests.swift:44:14: error: value of type 'GridViewController' has no member 'searchQuery'
...（共 6 处编译错误）
error: fatalError
EXIT_CODE=1
```

新测试针对新 API（`grid.searchQuery` / `store.searchResults(for:)`）编写，修复前
无法编译 → 红。executed=0（编译失败，无测试可执行）。

### 3.2 绿（修复后）

```
$ swift test --package-path Packages/LaunchUI --filter "SearchRevisionSemanticsTests|SearchPageRestoreTests"
✔ Test "输入 p/ph/photo 不递增 displayRevision(修复前红)" passed
✔ Test "搜索状态变化不改变普通 PageVisual cache identity" passed
✔ Suite "Search revision semantics" passed after 0.230 seconds.
✔ Suite "Search page restore" passed after 0.167 seconds.
✔ Test run with 7 tests in 2 suites passed after 0.231 seconds.
EXIT_CODE=0
```

- 新测试 1（revision 语义）：`grid.searchQuery = "p"/"ph"/"photo"` + `refresh()` 后
  `store.displayRevision` 与基线相等（修复前每次键入 bumpRevision → 5 个 revision）；
  清空搜索退出搜索态同样不递增。
- 新测试 2（cache identity）：搜索前准备 working set 捕获 `pageVisualKeysForDiag`；
  进入搜索（photo）→ 改词（ph）→ 退出搜索 → 重新准备后键集合与搜索前**完全一致**
  （displayRevision 未变 → PageVisualKey 未失效）。修复前 revision 被搜索污染 →
  键含新 revision → 断言失败（红）。

## 4. 回归表（真实执行，exit code + executed/passed/failed）

| 套件/命令 | executed | passed | failed | exit code |
|---|---|---|---|---|
| SearchRevisionSemanticsTests（新增） | 2 | 2 | 0 | 0 |
| SearchPageRestoreTests（搜索恢复回归） | 5 | 5 | 0 | 0 |
| AppLibrarySurfaceNavigationTests（搜索恢复/圆点） | 14 | 14 | 0 | 0 |
| PageCompositorGridIntegrationTests（搜索 eligibility + T-013 A/B） | 33 | 33 | 0 | 0 |
| **LaunchUI 全量** | **496** | **496** | **0** | **0** |
| **LaunchCore 全量** | **200** | **200** | **0** | **0** |
| **LaunchPlatform 全量** | **149** | **149** | **0** | **0** |
| `git diff --check` | — | — | — | 0 |

计数说明：任务包基线 489/67（提交态）。当前工作树含并发任务 T-026 的既有未提交
测试（WallpaperProviderTests +5，同一 suite）+ 本任务新增 2 测试 1 suite →
496/68。本任务新增测试全部通过；既有 489 测试无一失败（搜索回归全绿 = 过滤行为
与修复前一致）。

环境：`CLANG_MODULE_CACHE_PATH=/private/tmp SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp`
（SwiftPM 缓存隔离）。本机后台负载高，全量套件耗时较长（LaunchUI 全量 22s 起），
未误判超时。

## 5. 验收对照（任务包 §5 逐条）

1. **新增 revision 语义测试红→绿** ✓ — §3：修复前编译失败（EXIT_CODE=1），修复后
   2/2 通过；断言"输入 p/ph/photo 不递增 displayRevision"与"搜索状态变化不改变
   普通 PageVisual cache identity"。
2. **搜索功能回归全绿（行为不变）** ✓ — SearchPageRestoreTests 5/5、
   AppLibrarySurfaceNavigationTests 14/14、PageCompositorGridIntegrationTests 33/33
   （含搜索 eligibility：进入搜索 purge 缓存、退出恢复页 1）；全量 496/496 无失败。
3. **全量回归全绿** ✓ — LaunchUI 496/496（68 suites）、LaunchCore 200/16、
   LaunchPlatform 149/23、`git diff --check` exit 0。
4. **T-013 保护条件零违反（paging/compositor 零改动）** ✓ — 见 §6。
5. **生产 diff 可辨识（仅 searchQuery 归属 + revision 语义）** ✓ — §2.1：生产改动
   仅 LauncherStoring/LauncherStore/GridViewController/LauncherWindowController +
   诊断探针；`refresh()` 中 compositor shutdown 仍只在 `revisionChanged` 分支触发
   （搜索变化不 shutdown 普通页 compositor；进入搜索态由 `enterSearchMode` 既有
   路径 shutdown+purge，行为不变）。

## 6. T-013 逐条确认

- **T-013 A（lastPageActiveSettleInterruptionUsesLiveFallback）**：零改动。
  `PageCompositorGridIntegrationTests` 仅测试替身签名适配（searchResults 改名 +
  grid.searchQuery 设置），测试断言与生产路径未动；全量运行该测试 PASS。
- **T-013 B1（startSettleReplacementKeepsTelemetrySessionsIsolated）**：零改动，
  全量 PASS。
- **T-013 B2（settlingCancelledEventFlushesOnce）**：零改动，全量 PASS。
- **T-013 生产变更门**：本任务未触碰 `PageCompositor.swift`、
  `PagingInteractionController.swift`、`PageVisual.swift`、`PageVisualCache.swift`、
  `PageVisualRenderer.swift`、`PageSnapAnimator.swift`（git diff 验证：无这些文件
  的改动；`git diff --name-only` 中 PageCompositor/PagingInteraction/PageVisual
  匹配为空）。`GridViewController.refresh()` 的 compositor shutdown 调用点保持
  原语义（仅 revision 变化时 shutdown；搜索变化走 `enterSearchMode` 既有 shutdown
  路径）。
- **T-013 记录**：`Workflow/results/T-013.md` 未改写；本任务仅新增
  `Workflow/results/T-025.md`（Workflow 记录，任务包允许）。

## 7. 实际命令（exit code 汇总）

```
swift test --package-path Packages/LaunchUI --filter SearchRevisionSemanticsTests        # 红: EXIT_CODE=1(编译失败, 6 errors)
swift test --package-path Packages/LaunchUI --filter "SearchRevisionSemanticsTests|SearchPageRestoreTests"  # 绿: 7/7, exit 0
swift test --package-path Packages/LaunchUI --filter "AppLibrarySurfaceNavigationTests|PageCompositorGridIntegrationTests"  # 47/47, exit 0
swift test --package-path Packages/LaunchUI        # 496/496 (68 suites), exit 0
swift test --package-path Packages/LaunchCore      # 200/200 (16 suites), exit 0
swift test --package-path Packages/LaunchPlatform  # 149/149 (23 suites), exit 0
git diff --check                                   # exit 0
xcodebuild -project LaunchBetter.xcodeproj -scheme LaunchBetter -configuration Release -derivedDataPath /tmp/launchbetter-t025-derived build  # ** BUILD SUCCEEDED **, exit 0
```

## 8. Release 重建

```
** BUILD SUCCEEDED **
XCODEBUILD_EXIT=0
产物: /tmp/launchbetter-t025-derived/Build/Products/Release/LaunchBetter.app
```

## 9. 偏差

无。一处设计说明：`refresh()` 中 `hostItem.refreshModel()` 与语言刷新保留在
`revisionChanged || searchChanged` 合并分支内（修复前搜索键入也走该路径，行为
不变；`updateModel` 幂等，搜索变化重复调用为 no-op）。

## 10. Commit / push / tag / release

**未执行。** 工作树脏状态（含并发任务 T-026 的既有改动）原样保留，未提交、
未推送、未打 tag、未发布。

## 11. Reviewer 关注点

- 生产 diff 唯一性：仅搜索相关文件（§2.1），PageCompositor/PagingInteraction/
  PageVisual 零改动（§6）。
- revision 语义测试真实性：红 = 编译失败（新 API 不存在），绿 = 2/2 断言
  revision 不递增 + cache identity 不变（§3）。
- 搜索功能行为不变：搜索回归套件全绿 + 全量 496/496（§4）。
- 全量回归真实性：三包全量 + diff --check + Release BUILD SUCCEEDED（§4/§8）。
