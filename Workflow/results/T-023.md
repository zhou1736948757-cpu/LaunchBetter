# T-023 — raster 截断省略号对齐（显式 "…" token）

- 日期：2026-08-27
- 角色：Worker（T-023，EXECUTING）
- 基线：`HEAD == 9e7c768`（v0.7.1）；working tree 含 T-020/T-022 未提交改动（与 label 截断路径无关的常量引用 + T-022 截断宽度）
- 方案：Chief 裁定采纳显式 "…"（U+2026）token；**7.5 常量不变**（width 参数 = 结果行总宽上限含 token，与 live 同构）

## 1. 改动文件与 diff

### 生产代码：`Packages/LaunchUI/Sources/LaunchUI/PageVisualRenderer.swift`（仅 token 一行 + 注释）

| 位置 | 改动 |
|---|---|
| :545-549 | `labelTruncationInset` 常量注释补充 T-023 段：width 参数 = 结果行总宽上限（含 token），与 live "text+ellipsis ≤ frame−7.7" 同构 → 7.5 常量无需调整；实测 token@96.5 行宽 88.77/93.83pt ≈ live 88.5/93.5pt（Δ0.27/0.33pt 亚像素） |
| :616-630 | `drawCenteredText` 截断分支：创建 token 行 `CTLineCreateWithAttributedString("\u{2026}", 与文本同 attributes)`（同 font/foregroundColor），`CTLineCreateTruncatedLine(line, width, .end, tokenLine)`（nil → token） |

- **截断宽度不变**：`labelRect.width − 7.5`（cellSize=108 → 96.5），T-022 的 `truncationWidth` 参数与 `effectiveTruncationWidth` 逻辑零改动
- 占位字母路径（truncatingTail: false）不受影响（不传 truncationWidth、不截断）
- 文件夹标签走同一 `drawLabel` → 同路径自动同修（无额外改动）
- 缓存键：**未改**（token 是渲染常量，不进入 PageVisualKey）

### 测试：`Packages/LaunchUI/Tests/LaunchUITests/PageVisualRendererTests.swift`（+144 行，纯新增）

| 位置 | 新增 |
|---|---|
| :607 | `truncatedLineGlyphInfo(_:at:)` 辅助：复刻 drawCenteredText 截断管线（同 font/attributes/width/token），返回截断行 typographic 宽 + 末字形 + "…" 参考字形（字形级断言只能走复刻管线——渲染器私有无测试钩子；渲染器真实输出由像素断言覆盖） |
| :635 | `labelTailColumnRuns(_:width:height:cellSize:scale:)` 辅助：标签带内白像素列 run 序列 |
| :667 | `truncationTokenEllipsisBoundary`（T-023 边界用例，核心回归） |
| :729 | `truncationTokenWidthAlignsWithLive`（T-023 行宽对齐断言） |

### 未改动（T-013 保护）
`AppCellView.swift`（live 路径）、`PageCompositor.swift`、PageVisualKey/缓存签名、其他生产/测试文件。未 commit/push/tag/release。

## 2. 测试红→绿记录（真实执行）

### 红态（修复前 = nil token，仅新测试已加）
```
✘ Test "T-023 边界用例: 'Affinity Designer 2.6'@96.5 → 省略号(末字形 U+2026 + 像素 3 独立点 run), 行宽 ≤ 96.5"
   recorded an issue at PageVisualRendererTests.swift:706:9:
   Expectation failed: dotWidths.allSatisfy { $0 <= maxDotWidthPx }
✘ Test run with 1 test in 1 suite failed after 0.021 seconds with 1 issue.
EXIT=1
```
- 边界用例**红**：nil token 硬切 "Affinity Designer 2."（92.24pt，probe 实测），像素尾部列 run = …w11/w18/w5（"2" 11px + "." 5px，无 3 点 run）→ 3 独立点 run 断言失败
- 字形级断言（复刻管线带 token）红态下绿（它钉的是 CoreText 机制本身）；**红→绿由像素断言承载**（渲染器真实输出），两者都做、职责分离，见 §9
- 欠截断保护（T-022）红态下绿（保护性测试，修复前后均绿）

### 绿态（修复后 = 显式 token）
```
✔ Test run with 17 tests in 1 suite passed after 0.060 seconds.
EXIT=0
```
- 边界用例：末字形 == U+2026 字形（glyph 1446 == 1446）；像素尾部 3 个独立点 run（w5/w5/w5@4x，空隙 4-5px）；截断行宽 88.77 ≤ 96.5 ✓
- 行宽对齐：affinity 88.77 ∈ [88.0, 89.0]（live 88.5 ±0.5）、longname 93.83 ∈ [93.0, 94.0]（live 93.5 ±0.5）✓
- 欠截断保护：95.77 ≤ 96.5 → 完整绘制 ✓；T-019 三测试保持绿 ✓

### 阈值校准依据（确定性实测，非目测）
- 复刻管线（probe，与渲染器同 font/attributes/width/token）：affinity full=98.721 → truncated=88.770，末字形 1446 == "…" 字形 1446；nil token 末字形 1445 ≠ 1446（硬切证据）；longname full=113.799 → truncated=93.828；GarageBand full=95.771 ≤ 96.5 不截断（末字形 1310 ≠ 1446）
- 像素（真实 rasterize @4x，cellSize=108）：affinity ink 87.5pt，末 3 run w5/w5/w5（空隙 4-5px）；硬切版末 3 run w11/w18/w5 → 断言必挂
- 点宽阈值 2pt（8px@4x）：实测点 5px = 1.25pt，"2" run 11px 必挂；空隙阈值 ≥1px：实测 4-5px

## 3. 全量回归表（真实执行）

| 命令 | exit code | executed | passed | failed |
|---|---|---|---|---|
| `swift test --package-path Packages/LaunchUI --filter PageVisualRendererTests` | 0 | 17 | 17 | 0 |
| `swift test --package-path Packages/LaunchUI --filter PageCompositorTests` | 0 | 22 | 22 | 0 |
| `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` | 0 | 33 | 33 | 0 |
| `swift test --package-path Packages/LaunchUI` | 0 | 489（67 suites） | 489 | 0 |
| `swift test --package-path Packages/LaunchCore` | 0 | 200（16 suites） | 200 | 0 |
| `swift test --package-path Packages/LaunchPlatform` | 0 | 149（23 suites） | 149 | 0 |
| `git diff --check` | 0 | — | clean | — |

- LaunchUI 489 = 487 基线 + 2 新增（T-023）；无 exit 0 且 0 测试
- SwiftPM 缓存：`CLANG_MODULE_CACHE_PATH=/private/tmp SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp`

## 4. Release 重建

```bash
xcodebuild -project LaunchBetter.xcodeproj -scheme LaunchBetter -configuration Release -derivedDataPath /tmp/launchbetter-t023-derived build
```
→ **BUILD SUCCEEDED**（exit 0）；产物 `/tmp/launchbetter-t023-derived/Build/Products/Release/LaunchBetter.app`

## 5. PNG 证据（Workflow/evidence/T-023/）

生成方式：真实 `PageVisualRenderer.rasterize`（默认几何 cellSize=108/iconSize=80，2x），ImageIO 存 PNG；Python 像素解析标签带（y-down 88..101 → 2x 图像行 176..202，白像素 a>40 && r,g,b>200）。

| PNG | ink 宽 | 尾部 3 run（像素） | 空隙 | 判定 |
|---|---|---|---|---|
| `pagevisual_affinity_2x.png`（"Affinity Designer 2.6"） | 88.0pt | (181-184,w4) (186-189,w4) (192-194,w3) | 2/3px | **3 个独立点 run** ✓ |
| `pagevisual_longname_2x.png`（"Adobe Photoshop 2026"） | 93.0pt | (187-189,w3) (192-194,w3) (197-199,w3) | 3/3px | **3 个独立点 run** ✓ |

- live 对照（T-021 `live_affinity_2x.png`，同判据）：尾部 3 run (177-180,w4) (182-185,w4) (187-190,w4)，空隙 1-2px —— 结构一致（3 独立点 run）✓
- 修复前对照（probe 硬切渲染）："Affinity Designer 2." 92.24pt，尾部 run w11/w18/w5 —— 无 3 点 run（红态证据，§2）
- 探针源码：/tmp/T023/（probe.swift 复刻管线 + 真实渲染 + PNG 保存；hardcut.swift 硬切对照）

## 6. 验收对照（任务 §6 逐条）

1. **真实屏幕（主验收，用户）** ⏳ 待用户：compositor ON，滑动与停稳均显示 "Affinity Designer 2…"（省略号一致，无出现/消失）；"Adobe Photoshop 2026" 两态均 "Adobe Photoshop…"；`--disable-pagecompositor` A/B 佐证。本任务已交付修复后 Release app（§4），与 T-022 真实屏幕验收一并执行
2. **PNG 证据** ✓：`pagevisual_affinity_2x.png` 尾部 3 个独立点 run（与 `live_affinity_2x.png` 同判据，§5）
3. **全量回归** ✓：LaunchUI 489/489（+2 新增）、LaunchCore 200/16、LaunchPlatform 149/23、GridIntegration 33/33 全绿（§3）
4. **T-013 保护** ✓：生产 diff 仅 drawCenteredText token 一行 + 注释（§1）；live 路径/compositor/缓存键零改动（§7 逐条）

## 7. T-013 保护条件逐条确认

| 条件 | 状态 |
|---|---|
| PagingInteractionController 保持唯一 paging 运动写入者 | ✓ 未触碰 |
| PageCompositor 仅 presentation | ✓ 未触碰 |
| 不改 spring/stiffness/damping/fling/rubber-band/settle 时长/默认 linear 1.3/normalizedDamped 默认/compositor 激活条件 | ✓ 未触碰 |
| 不改 100ms prepare debounce | ✓ 未触碰 |
| 不改 advanceRealClipBehindCover() | ✓ 未触碰 |
| 不得 sleep 掩盖 | ✓ 无 |
| 不得 max(0,count) 掩盖 | ✓ 无 |
| 不得改公开 API | ✓ 仅 private 函数内 token 一行 + private 常量注释 |
| 不得删历史 FAIL 记录 | ✓ 未删 |
| 不得 commit/push/tag/release | ✓ 未执行（需 Main 确认） |

## 8. 偏差与说明

- **字形级断言走复刻管线**：`drawCenteredText` 为 private 且任务禁止加测试钩子，lastGlyph 检查只能复刻渲染器截断管线（同 font/attributes/width/token）断言 CoreText 机制（token → 末字形 == U+2026）；渲染器真实输出由像素 3 点 run 断言覆盖（该断言红态实测失败，§2）。两者都做，职责分离：字形级钉机制、像素级钉真实输出
- 红态下字形级断言绿（复刻管线带 token 不依赖渲染器实现）—— 红→绿由像素断言承载，已在 §2 明示
- 未提交任何 git 操作；working tree 保持 T-020/T-022 既有改动 + 本任务改动

## 9. Reviewer 关注点

- 生产 diff 唯一性：仅 drawCenteredText token 一行 + 常量注释（§1 行号可查；T-020/T-022 改动为既有 working tree 内容）
- token 字体一致性：token 行与文本同 `attributes`（同 font + foregroundColor），省略号字形匹配（glyph 1446 实测）
- 边界测试真实性：lastGlyph==U+2026（复刻管线）+ 像素 3 点 run（真实渲染，红态实测失败于 :706），行宽 ≤ 96.5
- 行宽对齐断言：affinity 88.77 ∈ [88.0, 89.0]、longname 93.83 ∈ [93.0, 94.0]（live ±0.5pt 固定区间）
- 全量回归真实性：§3 逐命令 exit code + 计数；T-013 零违反（§7）
