# T-018 — Compositor 镜像根因修复（rasterizer contract）

- 任务包：`Workflow/tasks/T-018.md`
- Worker 完成时间：2026-08-27（本地）
- 工作流：chef-worker-reviewer-workflow v1.4
- **Status: COMPLETED（待 Reviewer 独立审核）**

---

## 1. Probe 证据（阶段 1，先验证根因，只读）

### 1.1 P1 — ImageIO 直接落盘 `PageVisual.image`（不经 CGContext 二次绘制）

探针（临时测试文件 `T018ProbeTests.swift`，运行后已删除；探针源码见 §1.4）：
`CGImageDestinationCreateWithURL` 直接把 `PageVisualRenderer.rasterize` 产出的
`PageVisual.image` 写 PNG；同时用 `image.dataProvider.data` **原始字节**采样
（row 0 = 图像顶部行，不经任何读取端补偿）。fixture 镜像不对称：红上/蓝下图标 +
方向字母 'T'（顶横条/下竖干）。

| 采样点（原始字节，row 0 = 顶部） | 修复前（p1-prefix/） | 修复后（根目录） | 期望 |
|---|---|---|---|
| 1x 图标区 top(60,4) / bottom(60,92) | **蓝 / 红** | 红 / 蓝 | 红 / 蓝 |
| 2x 图标区 top(120,8) / bottom(120,184) | **蓝 / 红** | 红 / 蓝 | 红 / 蓝 |
| 文件夹子图标 top(28,15) / bottom(28,40) | **蓝 / 红** | 红 / 蓝 | 红 / 蓝 |
| 'T' 字母图标 上半/下半白像素 | **208 / 604（横条落底部）** | 604 / 208（横条在顶部） | 上 > 下 |

- 修复前 PNG（镜像）：`Workflow/evidence/T-018/p1-prefix/p1-rasterize-1x.png`（视觉确认：
  图标块**上蓝下红**，标签 "T018" 在底部）、`p1-rasterize-2x.png`、`p1-folder-1x.png`、
  `p1-rasterize-letterT-1x.png`；对照图（非 flipped）：`p1-fixture-icon-redtop-bluebottom.png`
  （红上蓝下）、`p1-fixture-letter-T.png`。
- 修复后 PNG（正向）：`Workflow/evidence/T-018/p1-rasterize-1x.png` 等（同目录覆盖为修复后版本）。
- 运行日志：`probe-run-prefix.log` / `probe-run-postfix.log`。

**结论：P1 成立 —— `PageVisual.image` 位图本身镜像（每个元素在正确位置上各自上下颠倒），
与 Chief 裁定一致（置信度 95% 获实证）。**

### 1.2 P3 — model+presentation 层树（滑动中 + 停稳）

真实 `GridViewController` + `NSWindow` + 真实 paging 引擎（确定性探针时钟），
合成带 phase 的 precise 滚动事件；沿 superlayer 到 window 根打印
`isGeometryFlipped / contentsAreFlipped / frame / bounds / position / transform /
sublayerTransform / contentsGravity`（model + presentation），滑动中（tracking，
compositor active）与停稳（idle，compositor 释放）各一次。日志：
`Workflow/evidence/T-018/p3-layer-tree.log`。

- **滑动中**：2 个 compositor 层（`contents != nil`，frame == baseFrame），链 =
  compositorLayer → 容器 view.layer → window 根。全部 `flipped=false`、
  `contentsFlipped=false`、`transform=identity`、`sublayerTransform=identity`、
  `gravity=resize`（model 与 presentation 一致）→ **anomalies=NONE**。
- **停稳**：live 链（collectionView → clip → scroll → 容器 → window 根）transform/
  sublayerTransform 全部 identity；live 文档路径上的 AppKit flipped 视图
  （collectionView/clip/scroll 的 `isGeometryFlipped`/`contentsAreFlipped`）为预期
  （已知正确路径，停稳时用户看到的内容正确），不计异常 → **anomalies=NONE**。

**结论：P3 成立 —— CA 链无 m22<0、无负 Y scale、无非 identity sublayerTransform，
排除 CA 链翻转。**

### 1.3 判定表核对

| 判定项 | 结果 | 结论 |
|---|---|---|
| P1 镜像（位图本身上下颠倒） | ✅ 成立（1x/2x/文件夹/字母全部镜像） | 进入阶段 2 |
| P3 无异常（无 m22<0/负 Y scale/非 identity sublayerTransform） | ✅ 成立（滑动中+停稳 anomalies=NONE） | 进入阶段 2 |

**判定表：P1 镜像 + P3 无异常 → 根因成立，进入修复。**（P2 真实屏幕红上验证与
P4 截图留档属用户配合项，见 §7 验收 #1。）

### 1.4 探针说明

探针为临时测试文件 `Packages/LaunchUI/Tests/LaunchUITests/T018ProbeTests.swift`
（P1 落盘 + P3 层树），`T018_EVIDENCE_DIR` 环境变量门控；运行后已删除，
最终 diff 无残留。证据（PNG/日志/像素采样）全部保留在 `Workflow/evidence/T-018/`。

---

## 2. 修复 diff（阶段 2）

### 2.1 生产改动（唯一生产文件：`Packages/LaunchUI/Sources/LaunchUI/PageVisualRenderer.swift`）

```diff
@@ rasterize() @@
-        // y-down 页面坐标(与 flipped 文档一致)。
-        context.translateBy(x: 0, y: CGFloat(pixelHeight))
+        // 绘制契约(T-018): 上下文为 y-up 设备坐标, 只按 scale 缩放 —— 不再做
+        // 全局 y 翻转。逻辑布局保持 y-down(与 flipped 文档一致, slotOrigin/frame/
+        // iconFrame/labelRect/childRect 计算不变), 在绘制边界用 quartzRect(topDown:
+        // canvasHeight:) 显式转换(y_quartz = canvasHeight − y_down − h)。这样
+        // CGContextDrawImage/CTLineDraw 在 y-up 下自然正向, 位图不再镜像。
+        let canvasHeight = CGFloat(pixelHeight) / CGFloat(scale)
         context.scaleBy(x: CGFloat(scale), y: CGFloat(scale))
-        context.scaleBy(x: 1, y: -1)

+        // 新增: y-down → y-up Quartz 显式转换
+        nonisolated private static func quartzRect(topDown: CGRect, canvasHeight: CGFloat) -> CGRect {
+            CGRect(x: topDown.minX, y: canvasHeight - topDown.maxY,
+                   width: topDown.width, height: topDown.height)
+        }

@@ drawAppIcon @@   iconFrame → quartzRect 后 draw/clip/fill/letter
@@ drawFolderThumbnail @@
-        // sheen: 顶亮 → 底暗(白 0.18 → 0.02)。
+        // sheen: 顶亮 → 底暗(白 0.18 → 0.02)。y-up 下顶部 = maxY、底部 = minY
+        // (与旧 y-down 的 minY→maxY 对调, 保持顶亮底暗)。
-                start: CGPoint(x: thumbFrame.midX, y: thumbFrame.minY),
-                end: CGPoint(x: thumbFrame.midX, y: thumbFrame.maxY),
+                start: CGPoint(x: quartz.midX, y: quartz.maxY),
+                end: CGPoint(x: quartz.midX, y: quartz.minY),
@@ 子图标网格 @@   childRect → quartzRect 后 clip/draw
@@ drawLabel @@   labelRect → quartzRect 后 drawCenteredText
@@ drawCenteredText @@
-        // 阴影(对齐 live: 黑 0.85, blur 4, 下方 1pt)。
+        // 阴影(对齐 live: 黑 0.85, blur 4, 下方 1pt)。y-up 上下文下"下方" = −y
-            offset: CGSize(width: 0, height: 1), blur: 4,
+            offset: CGSize(width: 0, height: -1), blur: 4,
-        // flipped(y-down)上下文绘制 CoreText 必须复位 textMatrix。
+        // y-up 上下文绘制 CoreText: textMatrix 保持 identity 即正向
```

要点（对照任务包 §3 允许清单逐条落实）：
- 移除全局翻转 `translateBy(0,pixelHeight)+scaleBy(1,-1)`；context 只 `scaleBy(scale)`（y-up）✅
- y-down 逻辑布局保留（slotOrigin/frame/iconFrame/labelRect/childRect 计算不变）✅
- 绘制边界显式转换 `quartzRect(topDown:canvasHeight:)`，`y_quartz = canvasHeight − y_down − h`，
  `canvasHeight = CGFloat(pixelHeight)/CGFloat(scale)`（亚像素精确）✅
- 阴影 offset `(0,1)→(0,−1)` ✅；文件夹渐变 start/end 对调（保持顶亮底暗）✅；
  textMatrix 保持 identity（y-up 下正确）✅

### 2.2 测试改动

- `PageVisualRendererTests.swift`（+188 行）：新增 5 个不对称方向测试
  （1x/2x 红上蓝下图标、'T' 字母图标、占位首字母 CTLineDraw 方向、文件夹子图标方向）
  + 3 个 helper（asymmetricIcon/letterTIcon/makeDirectionRequest/whiteCount）。
- `PageCompositorTests.swift`（+79 行）：新增 1 个不对称方向测试
  （红上/蓝下图标经真实 `activate` + `render(in:)` 未镜像）+ 2 个 helper。
- T-017 既有测试（`compositorLayerRendersUpright` 等）**原样保留为历史证据**，未删改。

### 2.3 禁止项核对

- 未改：PageCompositor.swift、PagingInteractionController/PagingSpring/PageSnapAnimator/
  PageVisualCache/GridViewController/AppCellView、LaunchCore/LaunchPlatform、
  Package.swift/Xcode 工程、其他测试文件。
- 未 commit/push/tag/release（工作树改动未暂存未提交）。

---

## 3. 测试红→绿记录（真实执行）

### 3.1 修复前（RED，方向测试全部失败 = 镜像症状）

```bash
swift test --package-path Packages/LaunchUI --filter PageVisualRendererTests
# ✘ 5 个 T-018 方向测试, 8 issues:
#   1x/2x 图标区顶部非红(蓝)、底部非蓝(红); 'T' 字母 topHalf=208 < bottomHalf=604;
#   占位首字母 topHalf=64 < bottomHalf=161; 文件夹子图标顶部非红
swift test --package-path Packages/LaunchUI --filter PageCompositorTests
# ✘ T-018 合成层方向测试, 2 issues(输出顶部非红、图标底部非蓝)
```

### 3.2 修复后（GREEN）

```bash
swift test --package-path Packages/LaunchUI --filter PageVisualRendererTests
# ✔ 9 tests(4 既有 + 5 新增)全绿
swift test --package-path Packages/LaunchUI --filter PageCompositorTests
# ✔ 22 tests(21 既有 + 1 新增)全绿
```

---

## 4. 全量回归（阶段 3，真实执行）

| # | 命令 | exit | executed | passed | failed | 基线对照 |
|---|---|---|---|---|---|---|
| 1 | `swift test --package-path Packages/LaunchUI --filter PageVisualRendererTests` | 0 | 9 | 9 | 0 | 4 既有 + 5 新增 |
| 2 | `swift test --package-path Packages/LaunchUI --filter PageCompositorTests` | 0 | 22 | 22 | 0 | 21 既有 + 1 新增 |
| 3 | `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` | 0 | 33 | 33 | 0 | 33/33 ✅ |
| 4 | `swift test --package-path Packages/LaunchUI` | 0 | 477 | 477 | 0 | 471/66 + 6 新增 ✅ |
| 5 | `swift test --package-path Packages/LaunchCore` | 0 | 200 | 200 | 0 | 200/16 ✅ |
| 6 | `swift test --package-path Packages/LaunchPlatform` | 0 | 149 | 149 | 0 | 149/23 ✅ |
| 7 | `git diff --check` | 0 | — | — | — | 无空白错误 |

- 无 `exit 0 且 executed == 0`（每项 executed > 0）。
- SwiftPM 缓存未报错，未触发 `CLANG_MODULE_CACHE_PATH` 前缀。

---

## 5. Release 重建（阶段 4）

```bash
xcodebuild -project LaunchBetter.xcodeproj -scheme LaunchBetter -configuration Release \
  -derivedDataPath /tmp/launchbetter-t018-derived build
# ** BUILD SUCCEEDED ** (exit 0)
# 产物: /tmp/launchbetter-t018-derived/Build/Products/Release/LaunchBetter.app
```

---

## 6. 验收对照（§7 逐条）

| # | 验收标准 | 状态 | 说明 |
|---|---|---|---|
| 1 | 真实屏幕（主验收）：compositor ON 滑动中图标+名字方向正确、位置不变；OFF 无回归 | **待用户确认** | 需用户手动确认 + P4 截图留档（MANUAL_PHYSICAL_GATE 保持 OPEN/REQUIRED）。代码侧证据：P1 修复后位图正向 + P3 合成链无翻转 + 合成层 render(in:) 方向测试绿 |
| 2 | 方向测试（辅助）：不对称 fixture 的 rasterize 位图方向 + compositor 层 render(in:) 方向 | ✅ 绿 | 5 个 rasterize 方向测试（1x/2x/字母/占位/文件夹）+ 1 个 compositor render(in:) 方向测试，修复前红（10 issues）→ 修复后全绿 |
| 3 | 全量回归：LaunchUI 471/66（+新增）、LaunchCore 200/16、LaunchPlatform 149/23、GridIntegration 33/33 全绿，exit 0、executed > 0 | ✅ | 477/66、200/16、149/23、33/33，全部 exit 0、executed > 0（§4 表） |
| 4 | T-013 保护条件零违反 | ✅ | 见 §7 逐条 |
| 5 | 证据链：P1/P3 结果、修复前后真实屏幕对比、测试红→绿记录 | ⚠️ 部分 | P1/P3 证据 ✅（Workflow/evidence/T-018/）、测试红→绿 ✅（§3）；修复前后**真实屏幕**对比待用户配合（P2/P4） |

---

## 7. T-013 保护条件逐条确认（§8，零违反）

| 保护条件 | 确认 |
|---|---|
| PagingInteractionController 保持唯一 paging 运动写入者 | ✅ 未触碰 |
| PageCompositor 仅 presentation | ✅ 未触碰 |
| 不改 spring/stiffness/damping/fling 阈值/rubber-band/settle 时长/默认 linear 1.3/normalizedDamped 默认/compositor 激活条件 | ✅ 未触碰 |
| 不改 100ms prepare debounce | ✅ 未触碰 |
| 不改 advanceRealClipBehindCover() | ✅ 未触碰 |
| 不用 sleep 掩盖 | ✅ 无 sleep 引入 |
| 不用 max(0,count) 掩盖 | ✅ 无 |
| 不改公开 API | ✅ 仅新增 private static 方法 + private 参数传递 |
| 不删历史 FAIL 记录 | ✅ T-017 测试与记录原样保留 |
| 不 commit/push/tag/release | ✅ 工作树未暂存未提交 |

---

## 8. 输出物清单

- `Workflow/evidence/T-018/`：P1 PNG（p1-prefix/ 修复前镜像版 + 根目录修复后正向版 +
  非 flipped 对照图）、P3 层树日志（p3-layer-tree.log）、probe 运行日志
  （probe-run-prefix.log / probe-run-postfix.log）。
- `Workflow/results/T-018.md`：本文件。
- Release app：`/tmp/launchbetter-t018-derived/Build/Products/Release/LaunchBetter.app`。

---

## 9. 遗留与不确定性（诚实记录）

1. **真实屏幕验收（§7 #1）未执行**：P2（canonical 红上/蓝下 PNG 直接赋 compositor
   layer.contents 观察真实屏幕）与 P4（WindowServer 截图留档）需用户配合；
   MANUAL_PHYSICAL_GATE 保持 OPEN/REQUIRED。代码侧证据链完整（P1 位图正向 +
   P3 合成链无翻转 + render(in:) 方向测试绿），但"真实屏幕不再镜像"最终以用户实测为准。
2. **P3 停稳链的 flipped 标记**：live 文档路径（collectionView/clip/scroll）的
   `isGeometryFlipped`/`contentsAreFlipped` 为 AppKit flipped 视图的预期值
   （停稳时用户看到的内容正确），不计为异常；合成链（滑动中）全部非 flipped。
3. **探针为临时文件**：`T018ProbeTests.swift` 运行后已删除（最终 diff 无残留），
   证据以落盘 PNG/日志为准；方向断言测试为永久测试，可随时复跑。
4. **视觉确认**：修复前 1x PNG 已人工视觉确认（上蓝下红 = 镜像）；修复后 PNG 以
   原始字节采样为准（红上蓝下 = 正向），未再做视觉复核。
