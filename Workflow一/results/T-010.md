> ## Historical status (T-013 C1 annotation, 2026-08-26)
>
> **SUPERSEDED BY T-010-R2.** This record is retained for audit history and is
> **not** the current acceptance result. The old B description ("correct Page 0
> completion") is outdated and is not the current fact: the covered-reverse
> target is Page 1, as verified by `Workflow/results/T-010-R2.md` and
> `Workflow/reviews/T-010-R2-r1.md` (PASS). C's last-page scenario was later
> replaced by the real mid-settle interruption evidence in T-013
> (`lastPageActiveSettleInterruptionUsesLiveFallback`); the T-010 C boundary
> wording here predates that evidence. Do not treat this file as current.

---

# T-010: 真实 active compositor interruption 集成测试

## Status
PASS

## Changes
- `PageCompositorGridIntegrationTests` now exercises the real `GridViewController` wiring with the deterministic probe clock.
- A covers a Page 1 → Page 2 compositor settle, advances to a non-boundary mid-offset, starts a non-zero same-direction interruption, proves immediate uncovered-coverage live fallback from the exact abort offset, then finishes at Page 3 with non-empty live content and no residual layers.
- B covers a mid-settle reverse whose target remains in the active working set. It asserts compositor reuse, stable layer object identities, hidden live content during reuse, continuous offset movement, no abort, correct Page 0 completion, and cleanup.
- C covers App Library and last-page out-of-bounds boundary interruption paths through the real live paging settle. It asserts non-empty live content, no missing/invalid page visual keys, correct final surface/page/clip, and no compositor residue.
- Added `PageCompositor.layerObjectIdentifiersForDiag` as a read-only diagnostic seam; production behavior is unchanged.

## Verification
- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` — PASS, 29 tests.
- `swift test --package-path Packages/LaunchUI --filter PagingInterruptResumeTests` — PASS, 12 tests.
- `git diff --check` — PASS.

## Evidence limitation
The compositor intentionally excludes App Library and out-of-bounds targets, so those boundary cases cannot truthfully be made into compositor-covered interruptions. C therefore verifies the real paging settle/interruption fallback path at those boundaries, including live visibility and invalid-visual prevention, rather than asserting an impossible active compositor state.

## Scope
No changes to PagingSpring, motion writer ownership, coordinate semantics, default feel, 100ms debounce, `advanceRealClipBehindCover()`, or production paging behavior. No commit, push, or release performed.
