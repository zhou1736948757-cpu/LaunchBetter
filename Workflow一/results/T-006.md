# T-006 Result — compositor interruption, telemetry lifecycle, damped chunk invariance, API boundary

## Outcome

Implemented the bounded T-006 packet on top of the current uncommitted T-005 worktree. No reset, clean, commit, push, tag, or release was performed. T-005's grid-origin placement fix remains intact.

## Changed files

### Production
- `Packages/LaunchUI/Sources/LaunchUI/PageCompositor.swift`
  - Added production `covers(pages:)` query independent of diagnostic page-index access.
  - Added the requested `diagnosticsEnabled` guard at the `applyOffset` event call site.
- `Packages/LaunchUI/Sources/LaunchUI/GridViewController.swift`
  - Reuses an active compositor only when it covers current and new target pages.
  - Uncovered interruption aborts before the first new offset write, synchronizes to compositor current offset, reveals live, and records live fallback without preparing/rasterizing.
  - Corrected App Library versus ordinary out-of-bounds classification.
  - Validates geometry, placement bounds, converted document rectangles, layer availability, and post-activation `isActive` before recording `.activated`.
  - Separates `--pagecompositor` metrics/events from `--pagingfeeltelemetry` activation telemetry.
  - Wires `beginShowSession()` from the launcher show lifecycle.
  - Flushes interrupted activation sessions before replacement and carries completion reason through summaries.
- `Packages/LaunchUI/Sources/LaunchUI/PagingInteractionController.swift`
  - Added one-shot `onWillInterruptSettle` callback before old settle state reset.
  - Added gesture-level raw displacement accumulation for `normalizedDamped`; linear remains the original deltaX × sensitivity accumulation path.
- `Packages/LaunchUI/Sources/LaunchUI/PageCompositorActivationTelemetry.swift`
  - Added immutable `Sendable` `GestureTelemetrySnapshot` and completion reasons.
  - MainActor now only snapshots/reset/ends the session; detached formatting computes sorting/reduce/p95/String formatting and returns summary delivery to MainActor.
  - Added explicit show-session reset.
- `Packages/LaunchUI/Sources/LaunchUI/PageCompositorActivationResult.swift`
  - Added active coverage reuse and interruption live-fallback results.
- `Packages/LaunchUI/Sources/LaunchUI/PagingFollowCurve.swift`
  - Moved curve implementation to LaunchUI as an internal Foundation/CoreGraphics-only type.
- `Packages/LaunchUI/Sources/LaunchUI/LauncherWindowController.swift`
  - Calls the grid show-session hook after the launcher is shown.

### Tests
- `Packages/LaunchUI/Tests/LaunchUITests/PageCompositorGridIntegrationTests.swift`
  - Updated interruption behavior test to verify uncovered target fallback and clip continuity.
- `Packages/LaunchUI/Tests/LaunchUITests/PageCompositorTests.swift`
  - Added production coverage-query behavior test independent of diagnostics.
- `Packages/LaunchUI/Tests/LaunchUITests/PageCompositorActivationTelemetryTests.swift`
  - Covers new result cases, async summary output, interrupted flush preservation/completion reason, show reset, and real DisplayLink interval separation.
- `Packages/LaunchUI/Tests/LaunchUITests/PagingFollowCurveTests.swift`
  - Migrated LaunchCore curve tests and added positive/negative/mixed/reversed and 60/120 Hz chunk-invariance behavior tests.
- Removed the old `Packages/LaunchCore/Sources/LaunchCore/PagingFollowCurve.swift` and `Packages/LaunchCore/Tests/LaunchCoreTests/PagingFollowCurveTests.swift` residuals so the accidental public API is not retained.

## Verification

| Command | Result |
|---|---|
| `swift test --package-path Packages/LaunchCore` | PASS — 97 tests, 0 failures |
| `swift test --package-path Packages/LaunchUI` | PASS — 458 tests, 0 failures |
| `swift test --package-path Packages/LaunchPlatform` | PASS — 149 tests, 0 failures |
| `git diff --check` | PASS — no whitespace errors |

LaunchUI had one intermediate compile failure while the new telemetry formatter still contained escaped format-string quotes; that source error was corrected before the final full-suite run. An existing integration expectation for the old always-reuse behavior was updated to the packet-required uncovered-target live fallback behavior; the final suite passed.

## Evidence / behavior notes

- Coverage is queried through `PageCompositor.covers(pages:)`, never through `pageIndicesForDiag` in production logic.
- `abort()` runs before tracking's first offset write and uses the compositor's `currentOffset` through the existing sync callback.
- `GestureTelemetrySnapshot` owns the interval array passed to detached formatting; no mutable recorder state is read from the detached task.
- `beginShowSession()` is only called by `LauncherWindowController.show()`, not by refresh, settle, or search transitions.
- `PagingFollowCurve` has no remaining source/API declaration under LaunchCore; the implementation and tests are internal to LaunchUI.
- The final verification does not establish subjective physical smoothness; that remains a manual gate.

## Deviations

- Existing unrelated dirty files (`AGENTS.md`, `MEMORY.md`, `PageVisualCache.swift`, and their pre-existing tests/workflow artifacts) were preserved and not reset or cleaned.
- The packet's existing `--pagingtelemetry` low-level file logger remains separate from the activation telemetry path; T-006's `--pagingfeeltelemetry` isolation is enforced for compositor metrics/events.

## Unresolved questions

- No unresolved code or test failures. Physical compositor feel and hardware DisplayLink behavior still require the manual gate below.

## MANUAL_PHYSICAL_GATE

Run the launcher on a real macOS display and verify with dense pages: covered reverse interruption, uncovered third-page interruption, last-page and App Library boundaries, no visible blank frame, continuous clip/live handoff, and 60/120 Hz behavior. Do not treat automated tests as proof of subjective smoothness.
