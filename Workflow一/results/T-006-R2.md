# T-006-R2 Results

## Repair

- Added an internal `SummaryFormatter` injection seam to `PageCompositorActivationTelemetry`.
- Production initialization still uses the default asynchronous `Task.detached` formatter; sorting, p95, reduction, and `String` formatting remain outside the final DisplayLink callback.
- Replaced the interruption test's sleep-based scheduling assumption with a real actor-backed formatter gate.
- The test flushes gesture 0 as interrupted, starts and flushes gesture 1, releases gesture 1 first, waits for formatter completion, asserts no summary has been delivered, then releases gesture 0 and asserts delivery order 0 then 1 with completion reasons preserved.
- No public API was added.

## Verification

- `swift test --package-path Packages/LaunchUI --filter PageCompositorActivationTelemetryTests` — PASS (6 tests, 0 failures).
- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests/pagingDiagnosticFlagsAreIndependent` — PASS (1 test, 0 failures).
- `swift test --package-path Packages/LaunchCore` — PASS (97 tests, 0 failures).
- `swift test --package-path Packages/LaunchUI` — PASS (459 tests, 0 failures).
- `swift test --package-path Packages/LaunchPlatform` — PASS (149 tests, 0 failures).
- `git diff --check` — PASS (no output / no whitespace errors).

No commit, push, or release was performed.

## Remaining Gate

The manual physical 60/120 Hz trackpad/display validation remains open from the prior review; this task only addresses deterministic asynchronous formatter-order test evidence.
