# T-006-R3 Results

## Changes

- Refactored `showSessionResetsFirstGestureMarker` to inject the deterministic `FormatterGate`, await formatter started/completed signals, and await explicit summary delivery via `AsyncStream`. No sleep-based timing remains in `PageCompositorActivationTelemetryTests.swift`.
- Replaced flag-only wiring assertions with behavior-driven coverage for `--pagecompositor`, `--pagingfeeltelemetry`, and both flags. Each case drives real `PageCompositor.activate/applyOffset`; compositor metrics/events are asserted for the compositor-enabled cases and absent otherwise. Feel telemetry cases drive display-link interval recording plus real activation-result flush and assert the emitted summary; pagecompositor-only asserts flush returns nil and no display-link record exists.

## Verification

- `swift test --package-path Packages/LaunchUI --filter PageCompositorActivationTelemetryTests` — PASS, 6 tests.
- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests/pagingDiagnosticFlagsAreIndependent` — PASS, 1 test.
- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` — PASS, 25 tests.
- `swift test --package-path Packages/LaunchCore` — PASS, 97 tests.
- `swift test --package-path Packages/LaunchUI` — PASS, 459 tests.
- `swift test --package-path Packages/LaunchPlatform` — PASS, 149 tests.
- `git diff --check` — PASS.
- Search `PageCompositorActivationTelemetryTests.swift` for `Task.sleep`/`sleep(` — no matches.

No commit, push, or release performed. Manual physical 60/120 Hz validation was not performed.
