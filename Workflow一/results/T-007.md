> ## HISTORICAL annotation (T-013 C2, 2026-08-26)
>
> This record predates the T-013 precise diff-attribution closure. Its statement
> "现有更广 production diff 属于已接受的 T-005/T-006 baseline" is overly broad:
> the working tree contains production work from several accepted tasks
> (T-005/T-006 baseline, T-008 telemetry lifecycle, T-009 monotonic counters,
> T-010 initial seams, T-011 event recorder). The precise per-task attribution
> is maintained in the canonical `Workflow/results/T-007.md`, which Main writes
> with the exact attribution after T-013 PASS; the full list is also recorded in
> `Workflow/results/T-013.md` (C2 section). Do not treat the broad-baseline
> sentence here as the current precise attribution.

---

# T-007: 全量验证与验收结论（已纠正）

## Status
PASS（自动化验收）。本轮原 T-007 结论因 telemetry 生命周期与 interruption 证据缺口被重新打开；T-008～T-012 已完成并经独立 Reviewer 通过。

## Final automated verification

- `swift test --package-path Packages/LaunchCore` — **200 tests / 16 suites PASS**
- `swift test --package-path Packages/LaunchUI` — **467 tests / 66 suites PASS**
- `swift test --package-path Packages/LaunchPlatform` — **149 tests / 23 suites PASS**
- `git diff --check` — **PASS**
- 未执行 commit、push、tag、release；dirty worktree 保留。

## Acceptance evidence

### T-008: telemetry lifecycle
- interrupted settle duration 使用当前 session 的实际 elapsed duration，不读取 stale `lastSettleDuration`。
- undefined cancellation duration 使用显式 nil 语义。
- shutdown、disable、jump、cancelled event 与 animator replacement 的 completion reason 明确为 settled/interrupted/cancelled；`liveFallback` 仅为 activation outcome。
- 每个 session 最多 flush 一次；异步 formatter 与 FIFO delivery 保持。
- tracking-only shutdown/disable/jump 的端到端 cancelled 覆盖见 `Workflow/reviews/T-008-R1-r1.md`。

### T-009: probe counters
- frame/write 使用单调总计数 + session baseline，reset 不制造负数。
- 第二次 probe gesture 与旧 session 计数隔离。
- 真实 DisplayLink interval 与 probe frame 保持区分。
- Reviewer PASS：`Workflow/reviews/T-009-r1.md`。

### T-010: real active compositor interruption integration
- **A missing coverage**：真实 GridViewController + PageCompositor 从 Page 1→Page 2 中途同向中断，目标 Page 3 缺 coverage 时立即 live fallback，最终真实到 Page 3，live 内容非空、offset 连续、无残留层。
- **B covered reverse**：真实 Page 1→Page 2 后 active placements 明确为 `[1,2]`；reverse resolved target 直接断言为 Page 1 且属于 active set；layer identity 稳定、live 保持隐藏、无 abort/rebuild；最终 exactly one `finishedSettle`、zero new `aborted`/`shutdown`，到 Page 1 并精确清理。
- **C boundaries**：App Library 与 last-page out-of-bounds 走 live fallback；live 内容非空且无 invalid page key。
- 当前依据为 `Workflow/reviews/T-010-R2-r1.md` PASS；较早 `Workflow/reviews/T-010-r1.md` FAIL 已由 R2 supersede，不是当前 blocker。

### T-011: callback order
- 真实 gesture 的统一事件序列严格满足：
  `directionCallback < compositorDecision < firstOffsetWrite`
- Reviewer PASS：`Workflow/reviews/T-011-r1.md`。

## Production / evidence distinction

- 本轮没有发现新的 production defect；修复内容解决了 telemetry 生命周期/计数语义，并补足了真实 integration evidence。
- 所有 protected constraints 保持：PagingInteractionController 仍是唯一 paging motion writer；默认 linear 1.3、PagingSpring、fling/rubber-band/settle 参数、normalizedDamped opt-in、100ms PageVisual prepare debounce、坐标语义、`advanceRealClipBehindCover()` 均未改动。
- T-010-R2 是 test/evidence-only repair；现有更广 production diff 属于已接受的 T-005/T-006 baseline，见 `Workflow/decisions/T-010-R1-scope.md`。

## MANUAL_PHYSICAL_GATE

**OPEN / REQUIRED**：本环境没有执行真实 macOS trackpad/mouse、真实 DisplayLink 60/120 Hz、硬件 telemetry 或主观 smoothness 验证。不得将自动化通过表述为真机手感验收完成。

## Final review

- `Workflow/reviews/T-012-r2.md` — **PASS**
- `Workflow/results/T-012.md` — current automated verification report
