# T-010-R2: 完成 covered reverse 证据

## Read first
- `Workflow/tasks/T-010-R1.md`
- `Workflow/reviews/T-010-R1-r1.md`
- `Workflow/decisions/T-010-R1-scope.md`
- actual test/source

## Required test-only repair
1. 通过已有 internal diagnostic seam 暴露/读取当前 `settleTargetPage`，在 reverse gesture 后直接断言 resolved target == Page 1；若没有可用 seam，新增最小 internal test-only forwarding seam，不得 public API。
2. 保存 reverse 前 `PageCompositor.Event` counts；最终 settle 后断言 exactly one new `.finishedSettle`, zero new `.aborted`, zero new `.shutdown`。
3. 保持 active placement set `[1,2]`、target membership、stable layer IDs、live hidden、offset continuity、final Page 1、exact cleanup、live nonempty。
4. 只修改测试和 Workflow 记录；不得修改生产 Swift 实现。若已有 seam 不足导致必须动生产，停止并报告 blocker，不要擅自扩架构。
5. 在结果中引用 `Workflow/decisions/T-010-R1-scope.md`，说明现有生产 diff 属于已接受的 T-005/T-006 baseline，不属于本修复。

## Verification
Run targeted integration/interruption tests, full LaunchUI, and `git diff --check`. Write `Workflow/results/T-010-R2.md`. No commit/push/release.