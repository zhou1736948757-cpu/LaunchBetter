# T-006-R3: 消除 telemetry sleep 并补充真实 flag 行为测试

## Blockers
详见 `.workflow/reviews/T-006-R2-r1.md`：
1. `pagingDiagnosticFlagsAreIndependent` 仅检查 enabled 状态，不驱动实际 behavior。
2. `PageCompositorActivationTelemetryTests.showSessionResetsFirstGestureMarker` 仍使用 Task.sleep，严格规则要求移除所有 sleep-based proof。

## Required Fix A: no sleep-based telemetry proof
- 为 show-session reset 测试注入同样的 deterministic SummaryFormatter/gate 或 completion latch。
- 测试必须由 gate/latch 明确等待 formatter started/completed，不使用 Task.sleep。
- 证明 beginShowSession 后 firstGestureSinceShow 的摘要字段正确变化。
- 全仓 telemetry test file 不得残留 `Task.sleep`/sleep-based timing proof。

## Required Fix B: real flag-isolation behavior test
增加真实行为测试，不只 assert enabled flags：
- `--pagecompositor` only：驱动一次 `PageCompositor.activate/applyOffset`，验证 compositor metrics/event 实际产生；确认 paging telemetry 不产生 summary/DisplayLink record。
- `--pagingfeeltelemetry` only：驱动一次 activation result/telemetry flush 或 display-link callback，验证 summary/telemetry 实际产生；确认 PageCompositor metrics/events 不产生。
- both：驱动两组行为，验证两者都产生。
使用现有 internal seam/configuration seam 和真实对象，不做源码字符串断言；不能依赖 command line 全局状态导致测试污染。
如测试层难以直接运行完整 Grid，可对 PageCompositor 与 Telemetry 的真实对象行为分别建立可控 harness，但必须验证实际 event/summary 结果，而非仅 enabled 属性。

## Preserve
- deterministic formatter gate and FIFO delivery from R2;
- T-005 coordinates, active interruption, chunk invariance, default linear, API boundary;
- async formatting outside final DisplayLink;
- no public API, no forbidden architecture/parameter changes;
- no commit/push/release.

## Verification
```bash
swift test --package-path Packages/LaunchUI --filter PageCompositorActivationTelemetryTests
swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests
swift test --package-path Packages/LaunchCore
swift test --package-path Packages/LaunchUI
swift test --package-path Packages/LaunchPlatform
git diff --check
```
Write `.workflow/results/T-006-R3.md` with actual results.