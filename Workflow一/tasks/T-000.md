# Task T-000 — Initialize workflow contract (v1.4)

- **Objective**: Re-initialize the `chef-worker-reviewer-workflow` contract for LaunchBetter at v1.4 after user-approved reconfiguration.
- **Owner**: Main (current main conversation)
- **Acceptance criteria**:
  1. `Workflow/config.json` is v1.4 schema with reconfigured models (`current-main-conversation` for main/chief/worker/reviewer), `max_worker_concurrency` 3, `thinking_depth` high.
  2. `AGENTS.md` managed section regenerated to v1.4 Main/Chief/Worker/Reviewer contract; all pre-existing user content preserved.
  3. `MEMORY.md` runtime-config block refreshed; managed version/role/state stamps updated; history preserved.
  4. `Workflow/manifest.json` version 1.4 with updated role assignments and artifact paths.
  5. `Workflow/STATE.json` created with status `READY`.
  6. `Workflow/PLAN.md` / `Workflow/MAIN_BRIEF.md` placeholders present.
  7. `.workflow/` legacy backup untouched (byte-identical).
- **Verification**: artifact inspection above; `git status` shows expected modified/untracked files.
- **Reviewer focus**: contract consistency between config.json / AGENTS.md / MEMORY.md / manifest.json; no user content loss.
