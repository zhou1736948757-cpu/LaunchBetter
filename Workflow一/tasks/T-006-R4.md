# T-006-R4: 消除直接相关 compositor/paging 测试的 sleep 依赖

## Blocker
`.workflow/reviews/T-006-R3-r1.md` 指出严格门禁仍发现：
- `PageCompositorGridIntegrationTests.swift` 的 `driveUntilIdle`、settle 中间态和 active interruption 场景使用 `Task.sleep`；
- `PagingInterruptResumeTests.swift` 的 settle/drive helper 使用 `Thread.sleep`。

## Required Scope
只处理上述直接相关的 compositor/paging 行为测试；不要清理整个仓库所有 sleep。

将这些测试改为确定性时间/帧推进 seam：
- 优先为 `PagingInteractionController` 测试提供 internal deterministic clock/time source 或 probe frame timestamp 注入，使每次 `probeDisplayFrame()` 前进固定 wall-clock 时间而无需 sleep；
- Grid integration 同理，使用现有可安全增加的 internal test seam；
- 不改变生产默认 clock、CADisplayLink、PagingSpring 或 settle 参数；测试 seam 必须只在测试构造中启用；
- 不能用 busy-loop 伪造时间；不能用 sleep 替代；不能弱化中间态/settle 断言。

保留并验证：
- T-006-R3 deterministic formatter gate/FIFO test；
- flag isolation 必须是真实 behavior test；
- T-005 坐标与 active interruption 等既有测试。

## Acceptance
- 直接相关测试文件中不再有 Task.sleep/Thread.sleep/sleep(；
- settle 中间态、直到 idle、interruption 行为仍用确定性时间推进且断言真实状态；
- 全量测试通过。

## Verification
```bash
swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests
swift test --package-path Packages/LaunchUI --filter PagingInterruptResumeTests
swift test --package-path Packages/LaunchCore
swift test --package-path Packages/LaunchUI
swift test --package-path Packages/LaunchPlatform
git diff --check
```
Write `.workflow/results/T-006-R4.md`. No commit/push/release.