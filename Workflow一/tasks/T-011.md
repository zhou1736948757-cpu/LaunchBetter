# T-011: 修复 callback 顺序测试

## Objective
将现有弱断言 `writes.count >= previousCount` 改为统一事件序列行为测试。

## Required
记录统一事件数组：
- `directionCallback`
- `compositorDecision`
- `firstOffsetWrite`

严格断言：
`directionCallback < compositorDecision < firstOffsetWrite`

测试必须真实驱动 GridViewController + PagingInteractionController；不得只依赖源码阅读。不得改变生产 ownership 或新增 motion writer。

## Constraints
不改默认 linear/PagingSpring/normalizedDamped opt-in、PageCompositor 坐标、100ms debounce、ownership、public API；不自动 commit/push/release。

## Verification
```bash
swift test --package-path Packages/LaunchUI --filter PagingDirectionCallbackTests
swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests
git diff --check
```

Write `Workflow/results/T-011.md`.