# T-010-R1: 修正 covered reverse 真实覆盖集合

## Blocker
`Workflow/reviews/T-010-r1.md`：测试从 Page 1→Page 2 形成 active placements `[1,2]` 后，却从 Page 1 反向到 Page 0；Page 0 不在覆盖集合，不能证明 covered reverse reuse。

## Required
修正真实 Grid + PageCompositor integration test：
1. 从 semantic Page 1 开始，真实启动 Page 1→Page 2 compositor settle，确认 active placement set 包含 `[1,2]`。
2. 推进到非边界 mid-settle；必要时让 current semantic page/offset 处于 Page 2 方向可逆状态，但不得伪造 coverage。
3. 第二次反向 tracking 的真实目标必须是 Page 1（仍在 active `[1,2]` 中），不是 Page 0。
4. 断言目标 page key 在 active placements 中；compositor 始终 active；abort count/teardown count 不增加；layer stable identifiers before/after 完全一致；live layer 保持隐藏；offset 连续；最终 Page 1 正确；收尾无层且 live 非空。
5. 测试不得只凭注释声称 coverage，必须断言 active pages 与目标页关系。

保留 T-010 A/C 测试和所有生产约束；不改 PageCompositor/PagingInteractionController 生产逻辑迎合错误测试；不改 PagingSpring/default linear/100ms debounce/ownership/API；不自动 commit/push/release。

运行：
```bash
swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests
swift test --package-path Packages/LaunchUI --filter PagingInterruptResumeTests
git diff --check
```
写 `Workflow/results/T-010-R1.md`。