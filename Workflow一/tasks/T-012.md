# T-012: 全量验证与 T-007 结论纠正

## Depends on
T-008, T-009, T-010, T-011。

## Required commands
```bash
swift test --package-path Packages/LaunchCore
swift test --package-path Packages/LaunchUI
swift test --package-path Packages/LaunchPlatform
git diff --check
git status --short --branch
```

## Acceptance
只有全部满足才可重新标记 T-007 PASS：
- interrupted duration 不陈旧；
- cancellation/replacement 生命周期明确；
- probe counters 非负且 session 不污染；
- missing coverage 测试真实完成到 Page 3；
- covered reverse 真实 compositor 集成测试；
- 三个 Swift package 全量通过；
- git diff --check clean。

最终报告明确区分：生产缺陷、测试证据缺口、自动测试、MANUAL_PHYSICAL_GATE、未执行真机验证。不得自动 commit/push/tag/release。

Write `Workflow/results/T-012.md` and update MEMORY task ledger/work log.