# T-001 Review Report

## Verdict: PASS

## Findings

### BLOCKER
None.

### MAJOR
None.

### MINOR
1. **`onWillBeginGesture` dead wiring**: The `onWillBeginGesture` property is still declared (line 43) and called in `beginGesture()` (line 337), but `GridViewController` no longer subscribes — it was replaced by `onWillStartHorizontalTracking`. The Worker result acknowledges this as intentional minimal change for later cleanup. It is harmless dead code (the closure is nil, so the call is a no-op), but it should be cleaned up in a future task to avoid confusion.

2. **T-003 `followCurve` co-located in diff**: The `PagingInteractionController` diff includes `followCurve` property and its usage in `feedTracking` (replacing `deltaX * PagingTuning.followSensitivity` with `followCurve.apply(...)`), plus the `--pagingfeel-damped` CLI flag in `GridViewController`. These are T-003 changes, not T-001. The default `.linear(sensitivity: PagingTuning.followSensitivity)` produces `rawDisplacement * sensitivity`, which is **bit-identical** to the old `deltaX * PagingTuning.followSensitivity`. Verified in `PagingFollowCurve.swift:26`. No behavioral change for T-001's scope. Noting for traceability — not a defect.

### QUESTION
None.

## Acceptance Criteria Verification

1. **Page 1 → Page 2 not blocked by currentPage == 0**: ✅ PASS
   - `compositorSurfaceGate()` removed the old `currentPage >= 1` guard (diff line: `currentPage >= 1` deleted).
   - `performWorkingSetPrepare` guard changed from `currentPage >= 1` to `currentPage >= 0`.
   - Test `firstPageToSecondActivatesTwoPageCompositor` confirms Page 0 → Page 1 activation.

2. **Boundary pages only need current + direction neighbor (2 visuals)**: ✅ PASS
   - `tryActivatePageCompositor(direction:)` requests `visuals(for: [currentPage, targetPage])` and checks `visuals.count == 2`.
   - No 3-visual requirement in the activation path.

3. **App Library direction → safe live fallback**: ✅ PASS
   - Leading layout page 0 with `.previous` → `targetPage = -1` → `guard targetPage >= 0` fails → return (live fallback).
   - `compositorSurfaceGate()` also checks `currentSurface != .appLibrary`.
   - Test `firstPageToLibraryFallsBack` confirms.

4. **No synchronous PageVisual rendering in gesture path**: ✅ PASS
   - `tryActivatePageCompositor` only reads from `pageVisualCache.visuals(for:)` (read-only, no LRU touch).
   - `performWorkingSetPrepare` is async, debounced 100ms, only runs when `phase == .idle`.
   - No `pageVisualRenderer.prepare()` call in any gesture callback.

5. **No new motion writer**: ✅ PASS
   - `routeScroll` remains the sole offset routing path. `PagingInteractionController.onScroll` is the only writer. No new `onScroll` or direct clip write introduced.

6. **PageVisualCache still max 3**: ✅ PASS
   - `maxVisualCount = 3` unchanged. `insert()` eviction loop still `while storage.count > Self.maxVisualCount`.
   - Test `explicitPagesDoesNotTouchLRUAndKeepsCap` confirms `visualCount == 3` after 3 inserts.

7. **PagingInteractionController still the only motion writer**: ✅ PASS
   - `PagingOffsetOwnership` tests pass (2/2). `routeScroll` routes to compositor or clip; no new writer.

8. **settle interruption not broken**: ✅ PASS
   - `beginGesture()` still interrupts settle: `animator.cancel()`, `interruptionCount += 1`, `interruptedSettleTarget = settleTargetPage`.
   - `PagingInterrupt` tests pass (11/11).
   - Direction callback fires after settle interruption, before first offset write — compositor activation respects `!pageCompositor.isActive` guard.

9. **advanceRealClipBehindCover() NOT deleted or changed**: ✅ PASS
   - Function at line 1396, unchanged in diff. `routeScroll` still calls it when `phase == .settling`.

10. **Search/drag/Folder/Settings/Library ownership unchanged**: ✅ PASS
    - `compositorSurfaceGate()` checks `!searchMode`, `!(dragController?.isDragging ?? false)`, `currentSurface != .appLibrary`.
    - No new ownership for Folder/Settings/Library. No changes to search/drag/folder/settings code paths.

11. **Public API unchanged**: ✅ PASS
    - New `PagingDirection` enum is `Sendable, Equatable` (value type, no API surface impact).
    - `onWillStartHorizontalTracking` is a new optional callback property (additive).
    - `PageVisualCache.visuals(for:)` is a new method (additive).
    - No existing public API removed or signature-changed.

12. **No new long-term owner or coordinator**: ✅ PASS
    - `PageVisualCache.visuals(for:)` is a read-only query, not a new owner.
    - No new scheduler, coordinator, or cache framework introduced.

13. **Direction callback called exactly once per gesture**: ✅ PASS
    - `hasEmittedDirectionCallback` reset in `beginGesture()`, set true on first emission.
    - Test `calledOncePerGesture` confirms: two gestures → two callbacks, each once.

14. **deltaX < 0 → next, deltaX > 0 → previous**: ✅ PASS
    - Code: `onWillStartHorizontalTracking?(deltaX < 0 ? .next : .previous)`.
    - Tests `negativeDeltaIsNext` and `positiveDeltaIsPrevious` confirm.

15. **Vertical/undetermined/zero-displacement do NOT trigger callback**: ✅ PASS
    - Callback is inside `feedTracking` after `axisLock.accumulate` returns true (horizontal locked).
    - `deltaX != 0` guard prevents zero-displacement emission.
    - Test `nonHorizontalGesturesDoNotCall` confirms all three cases.

16. **Page boundary check uses page width normalization (no hardcoded pixels)**: ✅ PASS
    - `isNearPageBoundary`: `let page = offset / pageWidth; let nearest = page.rounded(); return abs(page - nearest) <= 0.01`.
    - No hardcoded pixel values. 0.01 is a normalized tolerance.

17. **PageVisualCache.visuals(for:) returns in requested order, incompleteness detectable**: ✅ PASS
    - `pages.compactMap` preserves request order. Missing pages dropped → `count < pages.count` detects incompleteness.
    - Tests `explicitPages01InOrder`, `explicitPages12InOrder`, `explicitPagesDetectIncompleteness` confirm.

18. **performWorkingSetPrepare allows currentPage == 0, prepares [0,1] for first page**: ✅ PASS
    - Guard: `currentPage >= 0`. Page 0: `pages = [0, 1].filter { $0 < display.pages.count }`.
    - Test `firstPagePreparesTwoPages` confirms `pageVisualCacheCountForDiag == 2` and keys `[0, 1]`.

## Prohibited Changes Verification

1. **NSCollectionView NOT removed or rewritten**: ✅ VERIFIED — `collectionView` still used throughout. No removal.
2. **NSScrollView NOT removed**: ✅ VERIFIED — `enclosingScrollView` still used in `routeScroll`, `realClipOffset`, `onReadPageWidth`.
3. **No new PagerView or item view pool**: ✅ VERIFIED — No PagerView class, no item pool.
4. **No full hand-written view hierarchy for main grid**: ✅ VERIFIED — No view hierarchy rewrite.
5. **No AppCellView label Auto Layout rewrite**: ✅ VERIFIED — `AppCellView.swift` not in diff.
6. **No private SQLite database access**: ✅ VERIFIED — No SQLite references.
7. **No SLSSetMenuBarInsetAndAlpha or private SPI**: ✅ VERIFIED — No private SPI calls.
8. **No fixed 60Hz timer replacing CADisplayLink**: ✅ VERIFIED — `ensureDisplayLink` / `stopDisplayLink` still used.
9. **No second motion writer**: ✅ VERIFIED — `onScroll` is the sole writer closure.
10. **PagingSpring NOT replaced**: ✅ VERIFIED — `Packages/LaunchCore/` has no diff (only untracked `PagingFollowCurve.swift` from T-003).
11. **Window show/hide transition NOT rewritten**: ✅ VERIFIED — No show/hide transition changes.
12. **advanceRealClipBehindCover() NOT deleted**: ✅ VERIFIED — Present at line 1396, unchanged.
13. **No new general coordinator/scheduler/cache framework**: ✅ VERIFIED — No new framework classes.

## Test Results

```
swift test --package-path Packages/LaunchUI --filter PageCompositor
→ 37 tests in 2 suites passed (11.345s)

swift test --package-path Packages/LaunchUI --filter PagingDirection
→ 6 tests in 1 suite passed (0.005s)

swift test --package-path Packages/LaunchUI --filter PageVisualCache
→ 17 tests in 1 suite passed (0.005s)

swift test --package-path Packages/LaunchUI --filter PagingOffsetOwnership
→ 2 tests in 1 suite passed (0.020s)

swift test --package-path Packages/LaunchUI --filter PagingInterrupt
→ 11 tests in 1 suite passed (8.322s)

swift test --package-path Packages/LaunchUI (full suite)
→ 445 tests in 65 suites passed (32.957s)

swift test --package-path Packages/LaunchCore
→ 200 tests in 16 suites passed (0.105s)

git diff --check
→ clean (no whitespace errors)
```

## Recommendation
No repair actions needed. The implementation correctly satisfies all 18 acceptance criteria and violates none of the 13 prohibited changes. The two MINOR items (`onWillBeginGesture` dead wiring and T-003 `followCurve` co-location) are non-blocking and acknowledged by the Worker.