FAIL

# T-006-R3 independent strict review

## Verdict basis
The two R2 blockers were addressed in the dedicated telemetry test, and the new flag test drives real objects. However, the strict no-sleep rule is still violated by relevant compositor/paging tests. Because the request explicitly says any remaining sleep-based proof is FAIL, this repair is not accepted.

## Findings by severity

### BLOCKER
- **Sleep-based proof remains in relevant tests.** The requested search was run over the LaunchUI test tree. `PageCompositorActivationTelemetryTests.swift` itself has no `Task.sleep`, `sleep(`, or `Thread.sleep` matches. But the directly relevant `PageCompositorGridIntegrationTests.swift` still contains timing sleeps at lines 63 (`driveUntilIdle`), 334 (settle progression), and 829 (the active-interruption fallback test). The latter two are explicitly compositor/settle behavior proofs; line 63 is used by many compositor tests. The source comments explicitly describe real-time/timing dependence at lines 57 and 328. Under the stated acceptance rule, this is disqualifying. The broader relevant paging tests also contain `Thread.sleep` in `PagingInterruptResumeTests.swift:37,46,60`.

## Verified evidence

### R2 blocker 1: show-session reset gate/latch
- `PageCompositorActivationTelemetryTests.swift:5-47` defines `FormatterGate` with explicit started/completed waiters and release continuations.
- `showSessionResetsFirstGestureMarker` at lines 156-190 waits for formatter start (`175`, `183`), explicitly releases each formatter (`176`, `184`), waits for completion (`177`, `185`), then consumes summaries and asserts `firstGestureSinceShow=1` for both generations (`188-189`). No scheduler delay is used.
- `PageCompositorActivationTelemetry.swift:69-71` resets the marker via `beginShowSession`; `flushGesture` captures the marker before clearing it (`148-165`).

### R2 blocker 2: FIFO interruption ordering
- `PageCompositorActivationTelemetryTests.swift:109-154` deterministically waits for both formatter starts (`132`, `137`), releases gesture 1 first (`140`), waits for its completion (`141`), asserts no delivery (`142`), then releases gesture 0 (`145`) and awaits completion (`146`). It proves delivery order and completion reasons with assertions at `149-153`.
- `PageCompositorActivationTelemetry.swift:177-185` buffers completed summaries by gesture index and drains only contiguous indices, proving old-before-new delivery independent of formatter completion order.

### Real flag-isolation behavior
- `PageCompositorGridIntegrationTests.swift:981-1036` uses the injected `PagingDiagnosticsConfiguration(arguments:)` seam, not mutable process-wide command-line state.
- The pagecompositor-only case activates and applies offset on a real `PageCompositor` (`998-1005`), then asserts real metric/event output (`1007-1010`) and that telemetry flush is nil with zero intervals (`1031-1034`).
- The pagingfeeltelemetry-only case records two real interval callbacks and flushes a real activation result through the async formatter (`1016-1030`), while the compositor branch asserts zero metrics/events (`1011-1013`).
- The both-flags case executes both branches because the table includes both flags at `983-987`.
- Configuration is local per `GridViewController` and passed explicitly at `990-993`; no test mutates `CommandLine.arguments`.

### Preserved architecture/regression evidence
- Async formatting remains outside final DisplayLink: `PageCompositorActivationTelemetry.swift:141-173` captures an immutable snapshot and uses `Task.detached`; `PagingInteractionController.swift:560-563` invokes the interval hook before `processDisplayFrame`, while final formatting/delivery is not performed there.
- T-005 coordinate coverage remains in `PageCompositorGridIntegrationTests.swift:913-977`, including non-zero `gridOrigin`, page 0→1, middle, last, and leading-document-offset cases; the targeted integration suite passed.
- Active interruption fallback/reuse logic remains in `GridViewController.swift:1557-1577`, with `covers(pages:)` in `PageCompositor.swift:238-242`; the integration suite passed.
- `PagingInteractionController.swift:393-409` preserves default linear accumulation and applies normalizedDamped against gesture-total displacement; `PagingFollowCurveTests.swift` covers linear/default and chunk invariance cases. No new public declarations were found for the telemetry/configuration/curve types; the new seams are internal.

## Commands and actual results

- `rg -n -i 'task\\.sleep|sleep\\s*\\(' Packages/LaunchUI/Tests Packages/LaunchCore/Tests Packages/LaunchPlatform/Tests || true` — **FOUND** relevant LaunchUI sleeps, including `PageCompositorGridIntegrationTests.swift:63,334,829`; also found unrelated package sleeps. `PageCompositorActivationTelemetryTests.swift` had no matches.
- `swift test --package-path Packages/LaunchUI --filter PageCompositorActivationTelemetryTests` — PASS, 6 tests, 0 failures.
- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests/pagingDiagnosticFlagsAreIndependent` — PASS, 1 test, 0 failures.
- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` — PASS, 25 tests, 0 failures.
- `swift test --package-path Packages/LaunchCore` — PASS, 97 tests, 0 failures.
- `swift test --package-path Packages/LaunchUI` — PASS, 459 tests, 0 failures.
- `swift test --package-path Packages/LaunchPlatform` — PASS, 149 tests, 0 failures.
- `git diff --check` — PASS; no whitespace errors.

## MANUAL_PHYSICAL_GATE
REQUIRED: no real trackpad/display validation at 60/120 Hz was performed. Keep the manual physical gate open.
