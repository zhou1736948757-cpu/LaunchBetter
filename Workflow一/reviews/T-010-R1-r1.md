> ## Historical status (T-013 C1 annotation, 2026-08-26)
>
> **HISTORICAL — FAIL record**, retained for audit history; **not** the current
> acceptance result. Superseded by the T-010-R2 evidence repair (PASS):
> `Workflow/reviews/T-010-R2-r1.md` and `Workflow/results/T-010-R2.md`. Do not
> delete or rewrite this FAIL record.

---

FAIL

# LaunchBetter T-010-R1 independent review

## Evidence

- `Workflow/tasks/T-010-R1.md` requires the real GridViewController + PageCompositor covered reverse proof, plus preserved A/C, no production behavior/architecture/default changes, targeted tests, and `git diff --check`.
- The revised B test does genuinely start at semantic Page 1 and sends a real `pagingProbeGesture(deltaXs: [-180, -240, -120])` (Page 1 → Page 2). At `PageCompositorGridIntegrationTests.swift:477` it explicitly asserts active placements equal `[1, 2]`; at `:486-490` it performs the reverse gesture and asserts Page 1 is in the active placements. It also asserts active state, unchanged layer `ObjectIdentifier`s, hidden live layer, continuous offset, unchanged abort/teardown counts, final Page 1, empty compositor layers, visible live content, and passes the integration test.
- A remains covered by `interruptionFallsBackWhenCoverageMissing` (real Page 1 → Page 2 followed by uncovered Page 3 live fallback). C remains covered by `boundaryInterruptionsUseLiveFallback` for Library and last-page boundaries.
- Targeted verification passed: `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` (29 tests), `swift test --package-path Packages/LaunchUI --filter PagingInterruptResumeTests` (12 tests), and full package suite (467 tests / 66 suites). `git diff --check` passed.

## Blocking findings

1. The B test does not explicitly capture or assert the reverse gesture's resolved target page before settling. It asserts `activePagesBeforeReverse.contains(1)` and only later asserts `currentPageValue == 1`. That demonstrates the final result, but not as directly as the required “actual reverse target is Page 1 and target membership” proof. The comment claims `PagingTargetResolver` selects Page 1, but comments are not evidence.
2. The test does not assert exact final teardown cardinality. It compares teardown counts only before/after the reverse tracking frame, then checks inactive/empty/live-visible state after settle. It does not prove that final cleanup was exactly one normal `finishedSettle` teardown (and no abort/shutdown/rebuild) rather than merely ending in the same observable state.
3. The working tree diff includes substantial production changes in `GridViewController.swift`, `PagingInteractionController.swift`, `PageCompositor.swift`, `PageVisualCache.swift`, `PageSnapAnimator.swift`, and `LauncherWindowController.swift`, including activation/working-set behavior and lifecycle wiring. Although defaults are documented as preserved and all tests pass, this review cannot certify the task's explicit “no production behavior/architecture/default changes” constraint from the current diff; the repair should be isolated to the integration test (or provide an auditable prior-baseline justification for every production hunk).

## MANUAL_PHYSICAL_GATE

BLOCKED: A physical trackpad/mouse gesture run is still required. Before acceptance, make B directly record/assert the resolved reverse target as Page 1 and assert exactly one normal final teardown with no abort/shutdown/rebuild, then rerun both targeted suites and `git diff --check`. Also isolate or explicitly justify the production hunks; no implementation files were edited by this reviewer.
