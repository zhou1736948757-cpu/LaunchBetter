FAIL

# T-006 review r1

## Findings

- **BLOCKER** — LaunchUI full test suite is not green. `swift test --package-path Packages/LaunchUI` reproduced 458 tests with 2 issues in `PageCompositorActivationTelemetryTests.interruptionFlushPreservesOldSession`: the two detached formatting tasks can deliver summaries out of order (`PageCompositorActivationTelemetry.swift:151-155`). The observed output delivered gestureIndex=1 before the interrupted gestureIndex=0, so the test assertions at `PageCompositorActivationTelemetryTests.swift:79-80` fail. This directly violates acceptance criterion 13 and leaves interruption telemetry ordering unproven. The implementation does snapshot the old session before starting the new one, but it does not preserve FIFO summary delivery.
- **QUESTION** — No actual flag-wiring behavior test was added; the separation is visible in `GridViewController.swift:1364-1378`, but tests do not launch/configure under each command-line flag and assert metrics/events state. This is weaker evidence than the requested “verify actual flag wiring and tests.”
- **MINOR** — Physical trackpad/display validation was not performed; retain `MANUAL_PHYSICAL_GATE`.

## Criterion-by-criterion evidence

1. **Active compositor interruption / coverage:** PASS by inspection and integration tests. Production uses `PageCompositor.covers(pages:)` (`PageCompositor.swift:249-253`), not `pageIndicesForDiag` (the only production occurrence is the diagnostic accessor in `GridViewController.swift:1906`). Uncovered interruption calls `abort()` before the new tracking write, then records fallback (`GridViewController.swift:1538-1553`); compositor abort synchronizes/reveals/removes layers (`PageCompositor.swift:270-294`). Tests cover uncovered third-page fallback, clip continuity, residual layers, covered reversal, boundaries, and callback-before-write (`PageCompositorGridIntegrationTests.swift:325-369`, `PagingDirectionCallbackTests.swift:101-123`). No synchronous rasterization/cache wait is introduced in the interruption branch.
2. **T-005 coordinate fix:** PASS. Production adds `gridOrigin.x` and `gridOrigin.y` exactly once in the document rect (`GridViewController.swift:1609-1621`); leading offset is also added once. Non-zero-origin and leading-offset integration tests assert actual converted frames (`PageCompositorGridIntegrationTests.swift:824-930`).
3. **Telemetry snapshot/lifecycle:** FAIL overall because full suite fails as above. The snapshot is immutable `Sendable` and captured/reset on `@MainActor` (`PageCompositorActivationTelemetry.swift:12-25`, `125-150`); sorting/p95/reduce/String formatting is outside the recorder in `format` (`162-180`) and detached after flush (`151-155`). Interruption callback is installed before old settle reset (`GridViewController.swift:1329-1333`, `PagingInteractionController.swift:345-362`). However, detached tasks have no ordering mechanism, producing the concrete failure above. Completion reason plumbing is present (`GridViewController.swift:1671-1698`).
4. **Flag separation:** PASS by source inspection, QUESTION on behavioral test coverage. `--pagecompositor` alone controls compositor metrics/events (`GridViewController.swift:1361-1366`); `--pagingfeeltelemetry` only enables activation telemetry/DisplayLink interval callback (`1373-1378`). No test asserts both command-line configurations.
5. **firstGestureSinceShow:** PASS by inspection/tests. Reset is exposed at `GridViewController.swift:1292-1298`, called from real `LauncherWindowController.show()` (`LauncherWindowController.swift:796-801`), and recorder reset is documented as not refresh/settle (`PageCompositorActivationTelemetry.swift:52-55`).
6. **target<0 classification:** PASS. The guard classifies negative targets as library only when `leadingSurfaceEnabled` (`GridViewController.swift:1555-1562`), otherwise out-of-bounds.
7. **Activation validity:** PASS by inspection. Geometry, placements, layer availability and finite converted rects are checked (`GridViewController.swift:1570-1641`), and `.activated` is recorded only after `pageCompositor.isActive` (`1643-1648`).
8. **normalizedDamped invariance / linear compatibility:** PASS by source and tests. Gesture raw accumulation is reset and used only for non-linear curves (`PagingInteractionController.swift:366-370`, `396-409`); linear preserves deltaX*sensitivity accumulation (`396-400`). Tests cover one/two/ten, mixed, negative, reversal, 60/120-Hz chunking and linear 1.3 (`PagingFollowCurveTests.swift:119-145`).
9. **PagingFollowCurve API removal:** PASS. Implementation is internal in LaunchUI (`PagingFollowCurve.swift:4-8`); no LaunchCore tracked source/test residual remains (`git ls-files` returned none; grep found only LaunchUI references).
10. **applyOffset hot path:** PASS. Metrics timing/counting is guarded and applied-event construction is guarded (`PageCompositor.swift:213-245`); default diagnostics test passes (`PageCompositorTests.swift:252-272`).
11. **Forbidden architecture/parameter changes:** PASS by diff/source review. No changes were found to PagingSpring, fling/rubber-band/settle tuning, hide purge, or `advanceRealClipBehindCover`; that method remains in `GridViewController.swift:1428`.
12. **Tests are behavior tests:** PASS for the reviewed additions. Tests assert layer frames, clip writes, opacity/residual layers, counts, classifications, and curve values; they are not mere source-string checks. The telemetry test is a real asynchronous behavior test, and it currently exposes the ordering defect.
13. **All package tests:** FAIL. `swift test --package-path Packages/LaunchUI` failed: 458 tests, 2 issues. Targeted reproduction with `--filter PageCompositorActivationTelemetryTests/interruptionFlushPreservesOldSession` also failed with the same 2 issues. `swift test --package-path Packages/LaunchPlatform` passed 149 tests/0 failures. The chained Core/UI/Platform command stopped at UI; the task result's claimed 97/458/149 all-pass result is stale or not reproducible.
14. **git diff --check:** PASS, no whitespace errors.
15. **MANUAL_PHYSICAL_GATE:** REQUIRED and retained. No real trackpad validation was performed.
16. **No commit/push/tag/release:** PASS; repository remains uncommitted and no release operation was performed.
17. **No new public API:** PASS for PagingFollowCurve; it is internal LaunchUI and removed from LaunchCore.
18. **advanceRealClipBehindCover unchanged:** PASS by review; method remains present and no diff hunk changes it.
19. **PagingSpring/fling/rubber-band/settle parameters unchanged:** PASS by review; no LaunchCore diff and no relevant parameter edits in the reviewed diff.

## Test commands/results

- `swift test --package-path Packages/LaunchCore`: PASS per task result (97 tests, 0 failures); the chained run reached and passed Core before UI.
- `swift test --package-path Packages/LaunchUI`: **FAIL**, 458 tests, 2 issues, both telemetry summary ordering.
- `swift test --package-path Packages/LaunchUI --filter PageCompositorActivationTelemetryTests/interruptionFlushPreservesOldSession`: **FAIL**, 1 test, 2 issues; reproducible.
- `swift test --package-path Packages/LaunchPlatform`: PASS, 149 tests, 0 failures.
- `git diff --check`: PASS.

## Recommendation

Do not accept T-006. Serialize or otherwise sequence detached summary delivery so completion summaries retain gesture order (while keeping formatting off the final DisplayLink frame), then rerun the complete LaunchCore, LaunchUI, and LaunchPlatform suites. Add a behavioral flag-wiring test for `--pagingfeeltelemetry` versus `--pagecompositor`, and retain the manual physical gate for real trackpad/60-120 Hz validation.
