FAIL

# T-006-R2 independent final review

## Verdict basis
The repaired interruption-order proof is materially deterministic and satisfies the out-of-order formatter requirements, but the task is not accepted because the required flag-isolation evidence is not a real behavior test, and the telemetry test file still contains sleep-based asynchronous proof. The requested strict rule says any nondeterministic or sleep-based proof is FAIL.

## Findings by severity

### BLOCKER
- **Flag isolation is wiring/state inspection, not behavior validation.** `PageCompositorGridIntegrationTests.pagingDiagnosticFlagsAreIndependent` at `Packages/LaunchUI/Tests/LaunchUITests/PageCompositorGridIntegrationTests.swift:954-969` calls `applyPagingDiagnosticsConfiguration` and only asserts `metrics.enabled`, `diagnosticsEnabled`, `pageCompositorActivationTelemetry.enabled`, and whether `onDisplayLinkTick` is non-nil. It does not drive compositor offsets/frames or telemetry callbacks and inspect resulting events/summaries for each flag combination. The only matching coverage found by repository search is this test. This fails criterion 7's requirement for real behavior tests, even though the targeted command passes.
- **Sleep-based asynchronous proof remains in the telemetry test file.** `PageCompositorActivationTelemetryTests.showSessionResetsFirstGestureMarker` at `Packages/LaunchUI/Tests/LaunchUITests/PageCompositorActivationTelemetryTests.swift:156-172` uses `Task.sleep(10ms)` and `Task.sleep(20ms)` before asserting summary count/order. It leaves formatter scheduling and delivery timing to the runtime. The repaired interruption test itself no longer uses sleep, but under the explicit acceptance rule “Any nondeterministic or sleep-based proof is FAIL,” this remaining proof is disqualifying and should be replaced with a completion/latch seam too.

### PASS evidence
- **Real internal injection seam:** `PageCompositorActivationTelemetry.swift:31,36-46` defines an internal `SummaryFormatter` typealias, stores it privately, and provides an internal formatter initializer. No `public`/`open` declaration was added. Production `init()` at lines 38-42 keeps the default formatter; the seam is not a production public API.
- **Deterministic out-of-order completion:** `PageCompositorActivationTelemetryTests.swift:109-154` injects `FormatterGate`; both formatters are awaited at `waitUntilStarted`; it explicitly releases gesture 1 at lines 139-141, waits for completion, asserts `summaries.isEmpty` at line 142, then releases gesture 0 at lines 144-146. This proves gesture 1 formatting completes before gesture 0 and is buffered/not delivered.
- **Exact FIFO callback order and retained fields:** the same test asserts the first callback contains `gestureIndex=0` and `completionReason=interrupted`, the second contains `gestureIndex=1` and `completionReason=settled`, and `summaries.count == 2` at lines 147-154. Production `deliverSummary` buffers by index and drains contiguous indexes at `PageCompositorActivationTelemetry.swift:177-185`.
- **Async and outside final DisplayLink callback:** `flushGesture` captures the immutable snapshot and launches `Task.detached` at `PageCompositorActivationTelemetry.swift:141-173`; formatting is performed by the injected/default formatter and only delivery returns via `MainActor.run`. Sorting/p95/reduction/String formatting remain in `nonisolated static format` at lines 190-209. The DisplayLink wiring only records interval data at `GridViewController.swift:1319-1323`; flushes occur from interruption/idle lifecycle callbacks at lines 1359-1371, not inside the final DisplayLink tick.
- **Interruption preservation:** `PagingInteractionController.swift:345-362` invokes `onWillInterruptSettle` before cancelling/resetting old settle state. `GridViewController.swift:1359-1363` flushes the old telemetry session as interrupted before replacement state can overwrite it.
- **T-005 coordinates and active-interruption behavior:** full LaunchUI passed; the T-005 tests at `PageCompositorGridIntegrationTests.swift` cover non-zero `gridOrigin`, page 0→1, middle, last-page cases and leading offset once. The active interruption test (`live settle 中途 clip 不在页边界 → 不激活新 compositor`) also passed in the full suite.
- **normalizedDamped/default linear/API/architecture constraints:** `PagingFollowCurve.swift:6-18` is internal and preserves linear sensitivity; `PagingInteractionController.swift:393-409` keeps default linear accumulation and evaluates normalizedDamped against gesture-total displacement for chunk invariance. New telemetry/result types are internal. No public API expansion or forbidden synchronous formatting path was observed. No commit, push, or release was performed.

## Commands and actual results

- `swift test --package-path Packages/LaunchUI --filter PageCompositorActivationTelemetryTests` — PASS; Swift Testing reports 6 tests, 0 failures (the preceding XCTest selector reports 0 XCTest tests because these are Swift Testing tests).
- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests/pagingDiagnosticFlagsAreIndependent` — PASS; 1 test, 0 failures. This is passing wiring coverage, not sufficient real behavior coverage (finding above).
- `swift test --package-path Packages/LaunchCore` — PASS; 97 tests, 0 failures.
- `swift test --package-path Packages/LaunchUI` — PASS; 459 tests, 0 failures.
- `swift test --package-path Packages/LaunchPlatform` — PASS; 149 tests, 0 failures.
- `git diff --check` — PASS; no output / no whitespace errors.
- Repository search for `Task.sleep` in `PageCompositorActivationTelemetryTests.swift` — FOUND at lines 164 and 168, confirming residual sleep-based proof.
- Repository search for flag-isolation test coverage — only `pagingDiagnosticFlagsAreIndependent` was found; no behavior-driving flag test was found.

## MANUAL_PHYSICAL_GATE
REQUIRED: no real trackpad/display validation at 60/120 Hz was performed. Keep the manual physical gate open.
