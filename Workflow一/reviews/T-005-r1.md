# T-005 Review r1

## Verdict

PASS

## Findings

### BLOCKER

None.

### MAJOR

None.

### MINOR

None.

### QUESTION

None.

## Acceptance-criteria evidence

1. **Complete placement coordinates:** `GridViewController` constructs one `documentRect` with `leadingDocumentOffset`, `item.page * g.pageWidth`, `g.gridOrigin.x/y`, and `bounds` dimensions, then assigns `collectionView.convert(documentRect, to: view)` directly to `baseFrame`.
2. **No double-counting:** `leadingDocumentOffset` occurs once in the placement x expression and `page * pageWidth` occurs once. `gridOrigin.x` and `gridOrigin.y` each occur once. The leading-surface integration test independently expects one page-width leading offset and passed.
3. **Correct conversion:** The implementation converts the complete rect, preserving origin and size through the actual collection-view/view hierarchy rather than converting only a point.
4. **Renderer semantics preserved:** `PageVisualRenderer.swift` and `PageVisual.swift` have no T-005 diff. Renderer still subtracts `request.gridOrigin` while rasterizing and returns `logicalBounds` with zero origin and grid size. No logicalBounds masking change was introduced.
5. **Real coordinate assertions:** `PageCompositorGridIntegrationTests` asserts actual layer `minX`, `minY`, `width`, and `height` against independently converted expected frames, with nonzero `geometry.gridOrigin`. The scenarios cover 0→1, 1→2, and 2→3, plus a leading-surface case. The tests also assert page ordering and activation, but are not limited to counts/opacity/page index.
6. **Scope and boundaries:** The T-005 implementation is confined to placement construction and its integration tests; no reset, commit, push, release, or production-file edits were made by this review. The worktree contains other pre-existing uncommitted T-001/T-002/T-003-related changes in the same files, which were left untouched and are not required for this verdict.

## Test commands/results

- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` — **PASS**, 24 tests, 0 failures. Both T-005 coordinate tests passed, including all three page-transition scenarios and the leading-offset-once scenario.
- `git diff --check` — **PASS**, no whitespace errors.

## Recommendation

Accept T-005 as passing. The placement fix is mathematically aligned with the real grid content rect, preserves renderer/logicalBounds semantics, and is backed by real layer-frame integration assertions. Keep the unrelated uncommitted worktree changes out of the T-005 acceptance scope when integrating.
