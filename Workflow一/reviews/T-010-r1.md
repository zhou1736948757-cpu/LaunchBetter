> ## Historical status (T-013 C1 annotation, 2026-08-26)
>
> **HISTORICAL — FAIL record**, retained for audit history; **not** the current
> acceptance result. Superseded by the T-010-R2 evidence repair (PASS):
> `Workflow/reviews/T-010-R2-r1.md` and `Workflow/results/T-010-R2.md`. Do not
> delete or rewrite this FAIL record.

---

FAIL

# LaunchBetter T-010 Review (independent reviewer)

## Decision
FAIL. The required covered-reverse proof in B is not valid, despite the test suite passing.

## Evidence by requirement

### A — PASS
`Packages/LaunchUI/Tests/LaunchUITests/PageCompositorGridIntegrationTests.swift:354-397` uses the real `GridViewController` and `PageCompositor` wiring. It starts at semantic Page 1 (`:361-364`), performs a real Page 1→Page 2 gesture (`:366-370`), advances deterministic probe frames, saves `abortOffset` (`:370-373`), and uses a nonzero same-direction second gesture (`:375-378`). Immediate assertions cover inactive compositor, live opacity 1, clip continuity to the saved offset, nonempty live content, and unchanged cache count (`:379-387`). Completion asserts Page 3, exact offset, no layers, opacity 1, and nonempty live content (`:389-396`). No net-zero input or Page 2 final claim is used.

### B — FAIL (blocking finding)
The test does not prove that the reverse target remains covered. The setup explicitly starts at Page 1 and the first forward gesture is the Page 1→Page 2 case whose compositor placements are `[1, 2]` (`:258-285`, especially `:273-285`). The purported covered-reverse test then starts from Page 1 (`:448-451`) and asserts the final target is Page 0 (`:463-482`). Page 0 is not in the stated active `[Page 1, Page 2]` coverage; the comment at `:463-464` is internally contradictory. The test never asserts the active page indices immediately before/after the reverse against the target, and therefore does not establish “target remains covered.” Its layer identity assertion (`:457-469`) is consequently insufficient: it proves identity only on the observed path, not the required covered-target path. Per the task gate, missing layer/coverage identity proof is FAIL.

### C — PASS
The boundary test covers App Library and last-page out-of-bounds interruptions with real grid integration (`:853-920`). It asserts nonempty live content, unchanged cache keys, inactive compositor/live visibility, final valid page/clip, empty layers, and page-key bounds. The result's limitation that these targets are intentionally not compositor-covered is consistent with the task's boundary fallback requirement.

### D — PASS
The new layer identity seam is internal (`layerObjectIdentifiersForDiag`, `PageCompositor.swift` diff) and diagnostic-only. The production coverage query `covers(pages:)` does not write motion/offset state. The inspected wiring retains `PagingInteractionController` as the sole motion/offset writer; no public API expansion was found.

### E — PASS (with scope note)
No evidence in the inspected T-010 seams changes `PagingSpring`, coordinate semantics, default linear feel, 100ms debounce, ownership, or `advanceRealClipBehindCover()`. Deterministic timing is confined to probe APIs and production display-link timing remains wall-clock. The branch contains broader pre-existing diagnostic/telemetry changes; those are outside the narrow covered-reverse proof and should not be treated as T-010 acceptance evidence.

## Commands and results

- `swift test --package-path Packages/LaunchUI --filter PageCompositorGridIntegrationTests` — PASS, 29 tests.
- `swift test --package-path Packages/LaunchUI --filter PagingInterruptResumeTests` — PASS, 12 tests.
- `swift test --package-path Packages/LaunchUI` — PASS, 467 tests in 66 suites.
- `git diff --check` — PASS.

Passing tests do not cure the B evidence defect: the named test's scenario/comment and expected target are inconsistent with the required covered reverse target. No implementation files were edited.

## MANUAL_PHYSICAL_GATE
BLOCKED pending a corrected real GridViewController + PageCompositor integration test that begins Page 1→Page 2, interrupts mid-settle with a reverse gesture whose actual target is demonstrably in the active placement set, asserts the target page key and layer stable IDs before/after, asserts compositor never aborts/rebuilds, live remains hidden, offset is continuous, and cleanup is exact. Then rerun both targeted suites, the package suite, and `git diff --check`.
